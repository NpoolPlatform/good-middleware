// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/goodcoin"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// GoodCoinQuery is the builder for querying GoodCoin entities.
type GoodCoinQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.GoodCoin
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodCoinQuery builder.
func (gcq *GoodCoinQuery) Where(ps ...predicate.GoodCoin) *GoodCoinQuery {
	gcq.predicates = append(gcq.predicates, ps...)
	return gcq
}

// Limit adds a limit step to the query.
func (gcq *GoodCoinQuery) Limit(limit int) *GoodCoinQuery {
	gcq.limit = &limit
	return gcq
}

// Offset adds an offset step to the query.
func (gcq *GoodCoinQuery) Offset(offset int) *GoodCoinQuery {
	gcq.offset = &offset
	return gcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gcq *GoodCoinQuery) Unique(unique bool) *GoodCoinQuery {
	gcq.unique = &unique
	return gcq
}

// Order adds an order step to the query.
func (gcq *GoodCoinQuery) Order(o ...OrderFunc) *GoodCoinQuery {
	gcq.order = append(gcq.order, o...)
	return gcq
}

// First returns the first GoodCoin entity from the query.
// Returns a *NotFoundError when no GoodCoin was found.
func (gcq *GoodCoinQuery) First(ctx context.Context) (*GoodCoin, error) {
	nodes, err := gcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodcoin.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gcq *GoodCoinQuery) FirstX(ctx context.Context) *GoodCoin {
	node, err := gcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodCoin ID from the query.
// Returns a *NotFoundError when no GoodCoin ID was found.
func (gcq *GoodCoinQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = gcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodcoin.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gcq *GoodCoinQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := gcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodCoin entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodCoin entity is found.
// Returns a *NotFoundError when no GoodCoin entities are found.
func (gcq *GoodCoinQuery) Only(ctx context.Context) (*GoodCoin, error) {
	nodes, err := gcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodcoin.Label}
	default:
		return nil, &NotSingularError{goodcoin.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gcq *GoodCoinQuery) OnlyX(ctx context.Context) *GoodCoin {
	node, err := gcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodCoin ID in the query.
// Returns a *NotSingularError when more than one GoodCoin ID is found.
// Returns a *NotFoundError when no entities are found.
func (gcq *GoodCoinQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = gcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodcoin.Label}
	default:
		err = &NotSingularError{goodcoin.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gcq *GoodCoinQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := gcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodCoins.
func (gcq *GoodCoinQuery) All(ctx context.Context) ([]*GoodCoin, error) {
	if err := gcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return gcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (gcq *GoodCoinQuery) AllX(ctx context.Context) []*GoodCoin {
	nodes, err := gcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodCoin IDs.
func (gcq *GoodCoinQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := gcq.Select(goodcoin.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gcq *GoodCoinQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := gcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gcq *GoodCoinQuery) Count(ctx context.Context) (int, error) {
	if err := gcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return gcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (gcq *GoodCoinQuery) CountX(ctx context.Context) int {
	count, err := gcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gcq *GoodCoinQuery) Exist(ctx context.Context) (bool, error) {
	if err := gcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return gcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (gcq *GoodCoinQuery) ExistX(ctx context.Context) bool {
	exist, err := gcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodCoinQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gcq *GoodCoinQuery) Clone() *GoodCoinQuery {
	if gcq == nil {
		return nil
	}
	return &GoodCoinQuery{
		config:     gcq.config,
		limit:      gcq.limit,
		offset:     gcq.offset,
		order:      append([]OrderFunc{}, gcq.order...),
		predicates: append([]predicate.GoodCoin{}, gcq.predicates...),
		// clone intermediate query.
		sql:    gcq.sql.Clone(),
		path:   gcq.path,
		unique: gcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodCoin.Query().
//		GroupBy(goodcoin.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (gcq *GoodCoinQuery) GroupBy(field string, fields ...string) *GoodCoinGroupBy {
	grbuild := &GoodCoinGroupBy{config: gcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := gcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return gcq.sqlQuery(ctx), nil
	}
	grbuild.label = goodcoin.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.GoodCoin.Query().
//		Select(goodcoin.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (gcq *GoodCoinQuery) Select(fields ...string) *GoodCoinSelect {
	gcq.fields = append(gcq.fields, fields...)
	selbuild := &GoodCoinSelect{GoodCoinQuery: gcq}
	selbuild.label = goodcoin.Label
	selbuild.flds, selbuild.scan = &gcq.fields, selbuild.Scan
	return selbuild
}

func (gcq *GoodCoinQuery) prepareQuery(ctx context.Context) error {
	for _, f := range gcq.fields {
		if !goodcoin.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gcq.path != nil {
		prev, err := gcq.path(ctx)
		if err != nil {
			return err
		}
		gcq.sql = prev
	}
	if goodcoin.Policy == nil {
		return errors.New("ent: uninitialized goodcoin.Policy (forgotten import ent/runtime?)")
	}
	if err := goodcoin.Policy.EvalQuery(ctx, gcq); err != nil {
		return err
	}
	return nil
}

func (gcq *GoodCoinQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodCoin, error) {
	var (
		nodes = []*GoodCoin{}
		_spec = gcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*GoodCoin).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &GoodCoin{config: gcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(gcq.modifiers) > 0 {
		_spec.Modifiers = gcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gcq *GoodCoinQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gcq.querySpec()
	if len(gcq.modifiers) > 0 {
		_spec.Modifiers = gcq.modifiers
	}
	_spec.Node.Columns = gcq.fields
	if len(gcq.fields) > 0 {
		_spec.Unique = gcq.unique != nil && *gcq.unique
	}
	return sqlgraph.CountNodes(ctx, gcq.driver, _spec)
}

func (gcq *GoodCoinQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := gcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (gcq *GoodCoinQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   goodcoin.Table,
			Columns: goodcoin.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: goodcoin.FieldID,
			},
		},
		From:   gcq.sql,
		Unique: true,
	}
	if unique := gcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := gcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodcoin.FieldID)
		for i := range fields {
			if fields[i] != goodcoin.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gcq *GoodCoinQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gcq.driver.Dialect())
	t1 := builder.Table(goodcoin.Table)
	columns := gcq.fields
	if len(columns) == 0 {
		columns = goodcoin.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gcq.sql != nil {
		selector = gcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gcq.unique != nil && *gcq.unique {
		selector.Distinct()
	}
	for _, m := range gcq.modifiers {
		m(selector)
	}
	for _, p := range gcq.predicates {
		p(selector)
	}
	for _, p := range gcq.order {
		p(selector)
	}
	if offset := gcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (gcq *GoodCoinQuery) ForUpdate(opts ...sql.LockOption) *GoodCoinQuery {
	if gcq.driver.Dialect() == dialect.Postgres {
		gcq.Unique(false)
	}
	gcq.modifiers = append(gcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return gcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (gcq *GoodCoinQuery) ForShare(opts ...sql.LockOption) *GoodCoinQuery {
	if gcq.driver.Dialect() == dialect.Postgres {
		gcq.Unique(false)
	}
	gcq.modifiers = append(gcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return gcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gcq *GoodCoinQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodCoinSelect {
	gcq.modifiers = append(gcq.modifiers, modifiers...)
	return gcq.Select()
}

// GoodCoinGroupBy is the group-by builder for GoodCoin entities.
type GoodCoinGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gcgb *GoodCoinGroupBy) Aggregate(fns ...AggregateFunc) *GoodCoinGroupBy {
	gcgb.fns = append(gcgb.fns, fns...)
	return gcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (gcgb *GoodCoinGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := gcgb.path(ctx)
	if err != nil {
		return err
	}
	gcgb.sql = query
	return gcgb.sqlScan(ctx, v)
}

func (gcgb *GoodCoinGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range gcgb.fields {
		if !goodcoin.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := gcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (gcgb *GoodCoinGroupBy) sqlQuery() *sql.Selector {
	selector := gcgb.sql.Select()
	aggregation := make([]string, 0, len(gcgb.fns))
	for _, fn := range gcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(gcgb.fields)+len(gcgb.fns))
		for _, f := range gcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(gcgb.fields...)...)
}

// GoodCoinSelect is the builder for selecting fields of GoodCoin entities.
type GoodCoinSelect struct {
	*GoodCoinQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (gcs *GoodCoinSelect) Scan(ctx context.Context, v interface{}) error {
	if err := gcs.prepareQuery(ctx); err != nil {
		return err
	}
	gcs.sql = gcs.GoodCoinQuery.sqlQuery(ctx)
	return gcs.sqlScan(ctx, v)
}

func (gcs *GoodCoinSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := gcs.sql.Query()
	if err := gcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gcs *GoodCoinSelect) Modify(modifiers ...func(s *sql.Selector)) *GoodCoinSelect {
	gcs.modifiers = append(gcs.modifiers, modifiers...)
	return gcs
}
