// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/appgooddisplayname"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// AppGoodDisplayNameQuery is the builder for querying AppGoodDisplayName entities.
type AppGoodDisplayNameQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AppGoodDisplayName
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppGoodDisplayNameQuery builder.
func (agdnq *AppGoodDisplayNameQuery) Where(ps ...predicate.AppGoodDisplayName) *AppGoodDisplayNameQuery {
	agdnq.predicates = append(agdnq.predicates, ps...)
	return agdnq
}

// Limit adds a limit step to the query.
func (agdnq *AppGoodDisplayNameQuery) Limit(limit int) *AppGoodDisplayNameQuery {
	agdnq.limit = &limit
	return agdnq
}

// Offset adds an offset step to the query.
func (agdnq *AppGoodDisplayNameQuery) Offset(offset int) *AppGoodDisplayNameQuery {
	agdnq.offset = &offset
	return agdnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (agdnq *AppGoodDisplayNameQuery) Unique(unique bool) *AppGoodDisplayNameQuery {
	agdnq.unique = &unique
	return agdnq
}

// Order adds an order step to the query.
func (agdnq *AppGoodDisplayNameQuery) Order(o ...OrderFunc) *AppGoodDisplayNameQuery {
	agdnq.order = append(agdnq.order, o...)
	return agdnq
}

// First returns the first AppGoodDisplayName entity from the query.
// Returns a *NotFoundError when no AppGoodDisplayName was found.
func (agdnq *AppGoodDisplayNameQuery) First(ctx context.Context) (*AppGoodDisplayName, error) {
	nodes, err := agdnq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appgooddisplayname.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) FirstX(ctx context.Context) *AppGoodDisplayName {
	node, err := agdnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppGoodDisplayName ID from the query.
// Returns a *NotFoundError when no AppGoodDisplayName ID was found.
func (agdnq *AppGoodDisplayNameQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = agdnq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appgooddisplayname.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := agdnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppGoodDisplayName entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppGoodDisplayName entity is found.
// Returns a *NotFoundError when no AppGoodDisplayName entities are found.
func (agdnq *AppGoodDisplayNameQuery) Only(ctx context.Context) (*AppGoodDisplayName, error) {
	nodes, err := agdnq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appgooddisplayname.Label}
	default:
		return nil, &NotSingularError{appgooddisplayname.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) OnlyX(ctx context.Context) *AppGoodDisplayName {
	node, err := agdnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppGoodDisplayName ID in the query.
// Returns a *NotSingularError when more than one AppGoodDisplayName ID is found.
// Returns a *NotFoundError when no entities are found.
func (agdnq *AppGoodDisplayNameQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = agdnq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appgooddisplayname.Label}
	default:
		err = &NotSingularError{appgooddisplayname.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := agdnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppGoodDisplayNames.
func (agdnq *AppGoodDisplayNameQuery) All(ctx context.Context) ([]*AppGoodDisplayName, error) {
	if err := agdnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return agdnq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) AllX(ctx context.Context) []*AppGoodDisplayName {
	nodes, err := agdnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppGoodDisplayName IDs.
func (agdnq *AppGoodDisplayNameQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := agdnq.Select(appgooddisplayname.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := agdnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (agdnq *AppGoodDisplayNameQuery) Count(ctx context.Context) (int, error) {
	if err := agdnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return agdnq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) CountX(ctx context.Context) int {
	count, err := agdnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (agdnq *AppGoodDisplayNameQuery) Exist(ctx context.Context) (bool, error) {
	if err := agdnq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return agdnq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (agdnq *AppGoodDisplayNameQuery) ExistX(ctx context.Context) bool {
	exist, err := agdnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppGoodDisplayNameQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (agdnq *AppGoodDisplayNameQuery) Clone() *AppGoodDisplayNameQuery {
	if agdnq == nil {
		return nil
	}
	return &AppGoodDisplayNameQuery{
		config:     agdnq.config,
		limit:      agdnq.limit,
		offset:     agdnq.offset,
		order:      append([]OrderFunc{}, agdnq.order...),
		predicates: append([]predicate.AppGoodDisplayName{}, agdnq.predicates...),
		// clone intermediate query.
		sql:    agdnq.sql.Clone(),
		path:   agdnq.path,
		unique: agdnq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppGoodDisplayName.Query().
//		GroupBy(appgooddisplayname.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (agdnq *AppGoodDisplayNameQuery) GroupBy(field string, fields ...string) *AppGoodDisplayNameGroupBy {
	grbuild := &AppGoodDisplayNameGroupBy{config: agdnq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := agdnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return agdnq.sqlQuery(ctx), nil
	}
	grbuild.label = appgooddisplayname.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.AppGoodDisplayName.Query().
//		Select(appgooddisplayname.FieldCreatedAt).
//		Scan(ctx, &v)
func (agdnq *AppGoodDisplayNameQuery) Select(fields ...string) *AppGoodDisplayNameSelect {
	agdnq.fields = append(agdnq.fields, fields...)
	selbuild := &AppGoodDisplayNameSelect{AppGoodDisplayNameQuery: agdnq}
	selbuild.label = appgooddisplayname.Label
	selbuild.flds, selbuild.scan = &agdnq.fields, selbuild.Scan
	return selbuild
}

func (agdnq *AppGoodDisplayNameQuery) prepareQuery(ctx context.Context) error {
	for _, f := range agdnq.fields {
		if !appgooddisplayname.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if agdnq.path != nil {
		prev, err := agdnq.path(ctx)
		if err != nil {
			return err
		}
		agdnq.sql = prev
	}
	if appgooddisplayname.Policy == nil {
		return errors.New("ent: uninitialized appgooddisplayname.Policy (forgotten import ent/runtime?)")
	}
	if err := appgooddisplayname.Policy.EvalQuery(ctx, agdnq); err != nil {
		return err
	}
	return nil
}

func (agdnq *AppGoodDisplayNameQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppGoodDisplayName, error) {
	var (
		nodes = []*AppGoodDisplayName{}
		_spec = agdnq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*AppGoodDisplayName).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &AppGoodDisplayName{config: agdnq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(agdnq.modifiers) > 0 {
		_spec.Modifiers = agdnq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, agdnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (agdnq *AppGoodDisplayNameQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := agdnq.querySpec()
	if len(agdnq.modifiers) > 0 {
		_spec.Modifiers = agdnq.modifiers
	}
	_spec.Node.Columns = agdnq.fields
	if len(agdnq.fields) > 0 {
		_spec.Unique = agdnq.unique != nil && *agdnq.unique
	}
	return sqlgraph.CountNodes(ctx, agdnq.driver, _spec)
}

func (agdnq *AppGoodDisplayNameQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := agdnq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (agdnq *AppGoodDisplayNameQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   appgooddisplayname.Table,
			Columns: appgooddisplayname.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: appgooddisplayname.FieldID,
			},
		},
		From:   agdnq.sql,
		Unique: true,
	}
	if unique := agdnq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := agdnq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appgooddisplayname.FieldID)
		for i := range fields {
			if fields[i] != appgooddisplayname.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := agdnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := agdnq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := agdnq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := agdnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (agdnq *AppGoodDisplayNameQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(agdnq.driver.Dialect())
	t1 := builder.Table(appgooddisplayname.Table)
	columns := agdnq.fields
	if len(columns) == 0 {
		columns = appgooddisplayname.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if agdnq.sql != nil {
		selector = agdnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if agdnq.unique != nil && *agdnq.unique {
		selector.Distinct()
	}
	for _, m := range agdnq.modifiers {
		m(selector)
	}
	for _, p := range agdnq.predicates {
		p(selector)
	}
	for _, p := range agdnq.order {
		p(selector)
	}
	if offset := agdnq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := agdnq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (agdnq *AppGoodDisplayNameQuery) ForUpdate(opts ...sql.LockOption) *AppGoodDisplayNameQuery {
	if agdnq.driver.Dialect() == dialect.Postgres {
		agdnq.Unique(false)
	}
	agdnq.modifiers = append(agdnq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return agdnq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (agdnq *AppGoodDisplayNameQuery) ForShare(opts ...sql.LockOption) *AppGoodDisplayNameQuery {
	if agdnq.driver.Dialect() == dialect.Postgres {
		agdnq.Unique(false)
	}
	agdnq.modifiers = append(agdnq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return agdnq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (agdnq *AppGoodDisplayNameQuery) Modify(modifiers ...func(s *sql.Selector)) *AppGoodDisplayNameSelect {
	agdnq.modifiers = append(agdnq.modifiers, modifiers...)
	return agdnq.Select()
}

// AppGoodDisplayNameGroupBy is the group-by builder for AppGoodDisplayName entities.
type AppGoodDisplayNameGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (agdngb *AppGoodDisplayNameGroupBy) Aggregate(fns ...AggregateFunc) *AppGoodDisplayNameGroupBy {
	agdngb.fns = append(agdngb.fns, fns...)
	return agdngb
}

// Scan applies the group-by query and scans the result into the given value.
func (agdngb *AppGoodDisplayNameGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := agdngb.path(ctx)
	if err != nil {
		return err
	}
	agdngb.sql = query
	return agdngb.sqlScan(ctx, v)
}

func (agdngb *AppGoodDisplayNameGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range agdngb.fields {
		if !appgooddisplayname.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := agdngb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := agdngb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (agdngb *AppGoodDisplayNameGroupBy) sqlQuery() *sql.Selector {
	selector := agdngb.sql.Select()
	aggregation := make([]string, 0, len(agdngb.fns))
	for _, fn := range agdngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(agdngb.fields)+len(agdngb.fns))
		for _, f := range agdngb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(agdngb.fields...)...)
}

// AppGoodDisplayNameSelect is the builder for selecting fields of AppGoodDisplayName entities.
type AppGoodDisplayNameSelect struct {
	*AppGoodDisplayNameQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (agdns *AppGoodDisplayNameSelect) Scan(ctx context.Context, v interface{}) error {
	if err := agdns.prepareQuery(ctx); err != nil {
		return err
	}
	agdns.sql = agdns.AppGoodDisplayNameQuery.sqlQuery(ctx)
	return agdns.sqlScan(ctx, v)
}

func (agdns *AppGoodDisplayNameSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := agdns.sql.Query()
	if err := agdns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (agdns *AppGoodDisplayNameSelect) Modify(modifiers ...func(s *sql.Selector)) *AppGoodDisplayNameSelect {
	agdns.modifiers = append(agdns.modifiers, modifiers...)
	return agdns
}
