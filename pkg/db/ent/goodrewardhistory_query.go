// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/goodrewardhistory"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// GoodRewardHistoryQuery is the builder for querying GoodRewardHistory entities.
type GoodRewardHistoryQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.GoodRewardHistory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodRewardHistoryQuery builder.
func (grhq *GoodRewardHistoryQuery) Where(ps ...predicate.GoodRewardHistory) *GoodRewardHistoryQuery {
	grhq.predicates = append(grhq.predicates, ps...)
	return grhq
}

// Limit adds a limit step to the query.
func (grhq *GoodRewardHistoryQuery) Limit(limit int) *GoodRewardHistoryQuery {
	grhq.limit = &limit
	return grhq
}

// Offset adds an offset step to the query.
func (grhq *GoodRewardHistoryQuery) Offset(offset int) *GoodRewardHistoryQuery {
	grhq.offset = &offset
	return grhq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (grhq *GoodRewardHistoryQuery) Unique(unique bool) *GoodRewardHistoryQuery {
	grhq.unique = &unique
	return grhq
}

// Order adds an order step to the query.
func (grhq *GoodRewardHistoryQuery) Order(o ...OrderFunc) *GoodRewardHistoryQuery {
	grhq.order = append(grhq.order, o...)
	return grhq
}

// First returns the first GoodRewardHistory entity from the query.
// Returns a *NotFoundError when no GoodRewardHistory was found.
func (grhq *GoodRewardHistoryQuery) First(ctx context.Context) (*GoodRewardHistory, error) {
	nodes, err := grhq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodrewardhistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) FirstX(ctx context.Context) *GoodRewardHistory {
	node, err := grhq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodRewardHistory ID from the query.
// Returns a *NotFoundError when no GoodRewardHistory ID was found.
func (grhq *GoodRewardHistoryQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = grhq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodrewardhistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := grhq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodRewardHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodRewardHistory entity is found.
// Returns a *NotFoundError when no GoodRewardHistory entities are found.
func (grhq *GoodRewardHistoryQuery) Only(ctx context.Context) (*GoodRewardHistory, error) {
	nodes, err := grhq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodrewardhistory.Label}
	default:
		return nil, &NotSingularError{goodrewardhistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) OnlyX(ctx context.Context) *GoodRewardHistory {
	node, err := grhq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodRewardHistory ID in the query.
// Returns a *NotSingularError when more than one GoodRewardHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (grhq *GoodRewardHistoryQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = grhq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodrewardhistory.Label}
	default:
		err = &NotSingularError{goodrewardhistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := grhq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodRewardHistories.
func (grhq *GoodRewardHistoryQuery) All(ctx context.Context) ([]*GoodRewardHistory, error) {
	if err := grhq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return grhq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) AllX(ctx context.Context) []*GoodRewardHistory {
	nodes, err := grhq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodRewardHistory IDs.
func (grhq *GoodRewardHistoryQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := grhq.Select(goodrewardhistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := grhq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (grhq *GoodRewardHistoryQuery) Count(ctx context.Context) (int, error) {
	if err := grhq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return grhq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) CountX(ctx context.Context) int {
	count, err := grhq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (grhq *GoodRewardHistoryQuery) Exist(ctx context.Context) (bool, error) {
	if err := grhq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return grhq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (grhq *GoodRewardHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := grhq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodRewardHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (grhq *GoodRewardHistoryQuery) Clone() *GoodRewardHistoryQuery {
	if grhq == nil {
		return nil
	}
	return &GoodRewardHistoryQuery{
		config:     grhq.config,
		limit:      grhq.limit,
		offset:     grhq.offset,
		order:      append([]OrderFunc{}, grhq.order...),
		predicates: append([]predicate.GoodRewardHistory{}, grhq.predicates...),
		// clone intermediate query.
		sql:    grhq.sql.Clone(),
		path:   grhq.path,
		unique: grhq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodRewardHistory.Query().
//		GroupBy(goodrewardhistory.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (grhq *GoodRewardHistoryQuery) GroupBy(field string, fields ...string) *GoodRewardHistoryGroupBy {
	grbuild := &GoodRewardHistoryGroupBy{config: grhq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := grhq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return grhq.sqlQuery(ctx), nil
	}
	grbuild.label = goodrewardhistory.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.GoodRewardHistory.Query().
//		Select(goodrewardhistory.FieldCreatedAt).
//		Scan(ctx, &v)
func (grhq *GoodRewardHistoryQuery) Select(fields ...string) *GoodRewardHistorySelect {
	grhq.fields = append(grhq.fields, fields...)
	selbuild := &GoodRewardHistorySelect{GoodRewardHistoryQuery: grhq}
	selbuild.label = goodrewardhistory.Label
	selbuild.flds, selbuild.scan = &grhq.fields, selbuild.Scan
	return selbuild
}

func (grhq *GoodRewardHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, f := range grhq.fields {
		if !goodrewardhistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if grhq.path != nil {
		prev, err := grhq.path(ctx)
		if err != nil {
			return err
		}
		grhq.sql = prev
	}
	if goodrewardhistory.Policy == nil {
		return errors.New("ent: uninitialized goodrewardhistory.Policy (forgotten import ent/runtime?)")
	}
	if err := goodrewardhistory.Policy.EvalQuery(ctx, grhq); err != nil {
		return err
	}
	return nil
}

func (grhq *GoodRewardHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodRewardHistory, error) {
	var (
		nodes = []*GoodRewardHistory{}
		_spec = grhq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*GoodRewardHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &GoodRewardHistory{config: grhq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(grhq.modifiers) > 0 {
		_spec.Modifiers = grhq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, grhq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (grhq *GoodRewardHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := grhq.querySpec()
	if len(grhq.modifiers) > 0 {
		_spec.Modifiers = grhq.modifiers
	}
	_spec.Node.Columns = grhq.fields
	if len(grhq.fields) > 0 {
		_spec.Unique = grhq.unique != nil && *grhq.unique
	}
	return sqlgraph.CountNodes(ctx, grhq.driver, _spec)
}

func (grhq *GoodRewardHistoryQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := grhq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (grhq *GoodRewardHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   goodrewardhistory.Table,
			Columns: goodrewardhistory.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: goodrewardhistory.FieldID,
			},
		},
		From:   grhq.sql,
		Unique: true,
	}
	if unique := grhq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := grhq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodrewardhistory.FieldID)
		for i := range fields {
			if fields[i] != goodrewardhistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := grhq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := grhq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := grhq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := grhq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (grhq *GoodRewardHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(grhq.driver.Dialect())
	t1 := builder.Table(goodrewardhistory.Table)
	columns := grhq.fields
	if len(columns) == 0 {
		columns = goodrewardhistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if grhq.sql != nil {
		selector = grhq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if grhq.unique != nil && *grhq.unique {
		selector.Distinct()
	}
	for _, m := range grhq.modifiers {
		m(selector)
	}
	for _, p := range grhq.predicates {
		p(selector)
	}
	for _, p := range grhq.order {
		p(selector)
	}
	if offset := grhq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := grhq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (grhq *GoodRewardHistoryQuery) ForUpdate(opts ...sql.LockOption) *GoodRewardHistoryQuery {
	if grhq.driver.Dialect() == dialect.Postgres {
		grhq.Unique(false)
	}
	grhq.modifiers = append(grhq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return grhq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (grhq *GoodRewardHistoryQuery) ForShare(opts ...sql.LockOption) *GoodRewardHistoryQuery {
	if grhq.driver.Dialect() == dialect.Postgres {
		grhq.Unique(false)
	}
	grhq.modifiers = append(grhq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return grhq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (grhq *GoodRewardHistoryQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodRewardHistorySelect {
	grhq.modifiers = append(grhq.modifiers, modifiers...)
	return grhq.Select()
}

// GoodRewardHistoryGroupBy is the group-by builder for GoodRewardHistory entities.
type GoodRewardHistoryGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (grhgb *GoodRewardHistoryGroupBy) Aggregate(fns ...AggregateFunc) *GoodRewardHistoryGroupBy {
	grhgb.fns = append(grhgb.fns, fns...)
	return grhgb
}

// Scan applies the group-by query and scans the result into the given value.
func (grhgb *GoodRewardHistoryGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := grhgb.path(ctx)
	if err != nil {
		return err
	}
	grhgb.sql = query
	return grhgb.sqlScan(ctx, v)
}

func (grhgb *GoodRewardHistoryGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range grhgb.fields {
		if !goodrewardhistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := grhgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grhgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (grhgb *GoodRewardHistoryGroupBy) sqlQuery() *sql.Selector {
	selector := grhgb.sql.Select()
	aggregation := make([]string, 0, len(grhgb.fns))
	for _, fn := range grhgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(grhgb.fields)+len(grhgb.fns))
		for _, f := range grhgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(grhgb.fields...)...)
}

// GoodRewardHistorySelect is the builder for selecting fields of GoodRewardHistory entities.
type GoodRewardHistorySelect struct {
	*GoodRewardHistoryQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (grhs *GoodRewardHistorySelect) Scan(ctx context.Context, v interface{}) error {
	if err := grhs.prepareQuery(ctx); err != nil {
		return err
	}
	grhs.sql = grhs.GoodRewardHistoryQuery.sqlQuery(ctx)
	return grhs.sqlScan(ctx, v)
}

func (grhs *GoodRewardHistorySelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := grhs.sql.Query()
	if err := grhs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (grhs *GoodRewardHistorySelect) Modify(modifiers ...func(s *sql.Selector)) *GoodRewardHistorySelect {
	grhs.modifiers = append(grhs.modifiers, modifiers...)
	return grhs
}
