// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/goodreward"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
	"github.com/google/uuid"
)

// GoodRewardQuery is the builder for querying GoodReward entities.
type GoodRewardQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.GoodReward
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodRewardQuery builder.
func (grq *GoodRewardQuery) Where(ps ...predicate.GoodReward) *GoodRewardQuery {
	grq.predicates = append(grq.predicates, ps...)
	return grq
}

// Limit adds a limit step to the query.
func (grq *GoodRewardQuery) Limit(limit int) *GoodRewardQuery {
	grq.limit = &limit
	return grq
}

// Offset adds an offset step to the query.
func (grq *GoodRewardQuery) Offset(offset int) *GoodRewardQuery {
	grq.offset = &offset
	return grq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (grq *GoodRewardQuery) Unique(unique bool) *GoodRewardQuery {
	grq.unique = &unique
	return grq
}

// Order adds an order step to the query.
func (grq *GoodRewardQuery) Order(o ...OrderFunc) *GoodRewardQuery {
	grq.order = append(grq.order, o...)
	return grq
}

// First returns the first GoodReward entity from the query.
// Returns a *NotFoundError when no GoodReward was found.
func (grq *GoodRewardQuery) First(ctx context.Context) (*GoodReward, error) {
	nodes, err := grq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodreward.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (grq *GoodRewardQuery) FirstX(ctx context.Context) *GoodReward {
	node, err := grq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodReward ID from the query.
// Returns a *NotFoundError when no GoodReward ID was found.
func (grq *GoodRewardQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = grq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodreward.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (grq *GoodRewardQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := grq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodReward entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodReward entity is found.
// Returns a *NotFoundError when no GoodReward entities are found.
func (grq *GoodRewardQuery) Only(ctx context.Context) (*GoodReward, error) {
	nodes, err := grq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodreward.Label}
	default:
		return nil, &NotSingularError{goodreward.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (grq *GoodRewardQuery) OnlyX(ctx context.Context) *GoodReward {
	node, err := grq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodReward ID in the query.
// Returns a *NotSingularError when more than one GoodReward ID is found.
// Returns a *NotFoundError when no entities are found.
func (grq *GoodRewardQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = grq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodreward.Label}
	default:
		err = &NotSingularError{goodreward.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (grq *GoodRewardQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := grq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodRewards.
func (grq *GoodRewardQuery) All(ctx context.Context) ([]*GoodReward, error) {
	if err := grq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return grq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (grq *GoodRewardQuery) AllX(ctx context.Context) []*GoodReward {
	nodes, err := grq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodReward IDs.
func (grq *GoodRewardQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := grq.Select(goodreward.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (grq *GoodRewardQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := grq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (grq *GoodRewardQuery) Count(ctx context.Context) (int, error) {
	if err := grq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return grq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (grq *GoodRewardQuery) CountX(ctx context.Context) int {
	count, err := grq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (grq *GoodRewardQuery) Exist(ctx context.Context) (bool, error) {
	if err := grq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return grq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (grq *GoodRewardQuery) ExistX(ctx context.Context) bool {
	exist, err := grq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodRewardQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (grq *GoodRewardQuery) Clone() *GoodRewardQuery {
	if grq == nil {
		return nil
	}
	return &GoodRewardQuery{
		config:     grq.config,
		limit:      grq.limit,
		offset:     grq.offset,
		order:      append([]OrderFunc{}, grq.order...),
		predicates: append([]predicate.GoodReward{}, grq.predicates...),
		// clone intermediate query.
		sql:    grq.sql.Clone(),
		path:   grq.path,
		unique: grq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodReward.Query().
//		GroupBy(goodreward.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (grq *GoodRewardQuery) GroupBy(field string, fields ...string) *GoodRewardGroupBy {
	grbuild := &GoodRewardGroupBy{config: grq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := grq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return grq.sqlQuery(ctx), nil
	}
	grbuild.label = goodreward.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.GoodReward.Query().
//		Select(goodreward.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (grq *GoodRewardQuery) Select(fields ...string) *GoodRewardSelect {
	grq.fields = append(grq.fields, fields...)
	selbuild := &GoodRewardSelect{GoodRewardQuery: grq}
	selbuild.label = goodreward.Label
	selbuild.flds, selbuild.scan = &grq.fields, selbuild.Scan
	return selbuild
}

func (grq *GoodRewardQuery) prepareQuery(ctx context.Context) error {
	for _, f := range grq.fields {
		if !goodreward.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if grq.path != nil {
		prev, err := grq.path(ctx)
		if err != nil {
			return err
		}
		grq.sql = prev
	}
	if goodreward.Policy == nil {
		return errors.New("ent: uninitialized goodreward.Policy (forgotten import ent/runtime?)")
	}
	if err := goodreward.Policy.EvalQuery(ctx, grq); err != nil {
		return err
	}
	return nil
}

func (grq *GoodRewardQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodReward, error) {
	var (
		nodes = []*GoodReward{}
		_spec = grq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*GoodReward).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &GoodReward{config: grq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(grq.modifiers) > 0 {
		_spec.Modifiers = grq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, grq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (grq *GoodRewardQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := grq.querySpec()
	if len(grq.modifiers) > 0 {
		_spec.Modifiers = grq.modifiers
	}
	_spec.Node.Columns = grq.fields
	if len(grq.fields) > 0 {
		_spec.Unique = grq.unique != nil && *grq.unique
	}
	return sqlgraph.CountNodes(ctx, grq.driver, _spec)
}

func (grq *GoodRewardQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := grq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (grq *GoodRewardQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   goodreward.Table,
			Columns: goodreward.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: goodreward.FieldID,
			},
		},
		From:   grq.sql,
		Unique: true,
	}
	if unique := grq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := grq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodreward.FieldID)
		for i := range fields {
			if fields[i] != goodreward.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := grq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := grq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := grq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := grq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (grq *GoodRewardQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(grq.driver.Dialect())
	t1 := builder.Table(goodreward.Table)
	columns := grq.fields
	if len(columns) == 0 {
		columns = goodreward.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if grq.sql != nil {
		selector = grq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if grq.unique != nil && *grq.unique {
		selector.Distinct()
	}
	for _, m := range grq.modifiers {
		m(selector)
	}
	for _, p := range grq.predicates {
		p(selector)
	}
	for _, p := range grq.order {
		p(selector)
	}
	if offset := grq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := grq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (grq *GoodRewardQuery) ForUpdate(opts ...sql.LockOption) *GoodRewardQuery {
	if grq.driver.Dialect() == dialect.Postgres {
		grq.Unique(false)
	}
	grq.modifiers = append(grq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return grq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (grq *GoodRewardQuery) ForShare(opts ...sql.LockOption) *GoodRewardQuery {
	if grq.driver.Dialect() == dialect.Postgres {
		grq.Unique(false)
	}
	grq.modifiers = append(grq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return grq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (grq *GoodRewardQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodRewardSelect {
	grq.modifiers = append(grq.modifiers, modifiers...)
	return grq.Select()
}

// GoodRewardGroupBy is the group-by builder for GoodReward entities.
type GoodRewardGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (grgb *GoodRewardGroupBy) Aggregate(fns ...AggregateFunc) *GoodRewardGroupBy {
	grgb.fns = append(grgb.fns, fns...)
	return grgb
}

// Scan applies the group-by query and scans the result into the given value.
func (grgb *GoodRewardGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := grgb.path(ctx)
	if err != nil {
		return err
	}
	grgb.sql = query
	return grgb.sqlScan(ctx, v)
}

func (grgb *GoodRewardGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range grgb.fields {
		if !goodreward.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := grgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := grgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (grgb *GoodRewardGroupBy) sqlQuery() *sql.Selector {
	selector := grgb.sql.Select()
	aggregation := make([]string, 0, len(grgb.fns))
	for _, fn := range grgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(grgb.fields)+len(grgb.fns))
		for _, f := range grgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(grgb.fields...)...)
}

// GoodRewardSelect is the builder for selecting fields of GoodReward entities.
type GoodRewardSelect struct {
	*GoodRewardQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (grs *GoodRewardSelect) Scan(ctx context.Context, v interface{}) error {
	if err := grs.prepareQuery(ctx); err != nil {
		return err
	}
	grs.sql = grs.GoodRewardQuery.sqlQuery(ctx)
	return grs.sqlScan(ctx, v)
}

func (grs *GoodRewardSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := grs.sql.Query()
	if err := grs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (grs *GoodRewardSelect) Modify(modifiers ...func(s *sql.Selector)) *GoodRewardSelect {
	grs.modifiers = append(grs.modifiers, modifiers...)
	return grs
}
