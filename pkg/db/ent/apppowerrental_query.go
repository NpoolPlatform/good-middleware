// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/apppowerrental"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// AppPowerRentalQuery is the builder for querying AppPowerRental entities.
type AppPowerRentalQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AppPowerRental
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppPowerRentalQuery builder.
func (aprq *AppPowerRentalQuery) Where(ps ...predicate.AppPowerRental) *AppPowerRentalQuery {
	aprq.predicates = append(aprq.predicates, ps...)
	return aprq
}

// Limit adds a limit step to the query.
func (aprq *AppPowerRentalQuery) Limit(limit int) *AppPowerRentalQuery {
	aprq.limit = &limit
	return aprq
}

// Offset adds an offset step to the query.
func (aprq *AppPowerRentalQuery) Offset(offset int) *AppPowerRentalQuery {
	aprq.offset = &offset
	return aprq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aprq *AppPowerRentalQuery) Unique(unique bool) *AppPowerRentalQuery {
	aprq.unique = &unique
	return aprq
}

// Order adds an order step to the query.
func (aprq *AppPowerRentalQuery) Order(o ...OrderFunc) *AppPowerRentalQuery {
	aprq.order = append(aprq.order, o...)
	return aprq
}

// First returns the first AppPowerRental entity from the query.
// Returns a *NotFoundError when no AppPowerRental was found.
func (aprq *AppPowerRentalQuery) First(ctx context.Context) (*AppPowerRental, error) {
	nodes, err := aprq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{apppowerrental.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) FirstX(ctx context.Context) *AppPowerRental {
	node, err := aprq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppPowerRental ID from the query.
// Returns a *NotFoundError when no AppPowerRental ID was found.
func (aprq *AppPowerRentalQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = aprq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{apppowerrental.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := aprq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppPowerRental entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppPowerRental entity is found.
// Returns a *NotFoundError when no AppPowerRental entities are found.
func (aprq *AppPowerRentalQuery) Only(ctx context.Context) (*AppPowerRental, error) {
	nodes, err := aprq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{apppowerrental.Label}
	default:
		return nil, &NotSingularError{apppowerrental.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) OnlyX(ctx context.Context) *AppPowerRental {
	node, err := aprq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppPowerRental ID in the query.
// Returns a *NotSingularError when more than one AppPowerRental ID is found.
// Returns a *NotFoundError when no entities are found.
func (aprq *AppPowerRentalQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = aprq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{apppowerrental.Label}
	default:
		err = &NotSingularError{apppowerrental.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := aprq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppPowerRentals.
func (aprq *AppPowerRentalQuery) All(ctx context.Context) ([]*AppPowerRental, error) {
	if err := aprq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return aprq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) AllX(ctx context.Context) []*AppPowerRental {
	nodes, err := aprq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppPowerRental IDs.
func (aprq *AppPowerRentalQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := aprq.Select(apppowerrental.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := aprq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aprq *AppPowerRentalQuery) Count(ctx context.Context) (int, error) {
	if err := aprq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return aprq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) CountX(ctx context.Context) int {
	count, err := aprq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aprq *AppPowerRentalQuery) Exist(ctx context.Context) (bool, error) {
	if err := aprq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return aprq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (aprq *AppPowerRentalQuery) ExistX(ctx context.Context) bool {
	exist, err := aprq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppPowerRentalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aprq *AppPowerRentalQuery) Clone() *AppPowerRentalQuery {
	if aprq == nil {
		return nil
	}
	return &AppPowerRentalQuery{
		config:     aprq.config,
		limit:      aprq.limit,
		offset:     aprq.offset,
		order:      append([]OrderFunc{}, aprq.order...),
		predicates: append([]predicate.AppPowerRental{}, aprq.predicates...),
		// clone intermediate query.
		sql:    aprq.sql.Clone(),
		path:   aprq.path,
		unique: aprq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppPowerRental.Query().
//		GroupBy(apppowerrental.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (aprq *AppPowerRentalQuery) GroupBy(field string, fields ...string) *AppPowerRentalGroupBy {
	grbuild := &AppPowerRentalGroupBy{config: aprq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := aprq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return aprq.sqlQuery(ctx), nil
	}
	grbuild.label = apppowerrental.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.AppPowerRental.Query().
//		Select(apppowerrental.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (aprq *AppPowerRentalQuery) Select(fields ...string) *AppPowerRentalSelect {
	aprq.fields = append(aprq.fields, fields...)
	selbuild := &AppPowerRentalSelect{AppPowerRentalQuery: aprq}
	selbuild.label = apppowerrental.Label
	selbuild.flds, selbuild.scan = &aprq.fields, selbuild.Scan
	return selbuild
}

func (aprq *AppPowerRentalQuery) prepareQuery(ctx context.Context) error {
	for _, f := range aprq.fields {
		if !apppowerrental.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aprq.path != nil {
		prev, err := aprq.path(ctx)
		if err != nil {
			return err
		}
		aprq.sql = prev
	}
	if apppowerrental.Policy == nil {
		return errors.New("ent: uninitialized apppowerrental.Policy (forgotten import ent/runtime?)")
	}
	if err := apppowerrental.Policy.EvalQuery(ctx, aprq); err != nil {
		return err
	}
	return nil
}

func (aprq *AppPowerRentalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppPowerRental, error) {
	var (
		nodes = []*AppPowerRental{}
		_spec = aprq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*AppPowerRental).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &AppPowerRental{config: aprq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(aprq.modifiers) > 0 {
		_spec.Modifiers = aprq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aprq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aprq *AppPowerRentalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aprq.querySpec()
	if len(aprq.modifiers) > 0 {
		_spec.Modifiers = aprq.modifiers
	}
	_spec.Node.Columns = aprq.fields
	if len(aprq.fields) > 0 {
		_spec.Unique = aprq.unique != nil && *aprq.unique
	}
	return sqlgraph.CountNodes(ctx, aprq.driver, _spec)
}

func (aprq *AppPowerRentalQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := aprq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (aprq *AppPowerRentalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   apppowerrental.Table,
			Columns: apppowerrental.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: apppowerrental.FieldID,
			},
		},
		From:   aprq.sql,
		Unique: true,
	}
	if unique := aprq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := aprq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, apppowerrental.FieldID)
		for i := range fields {
			if fields[i] != apppowerrental.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aprq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aprq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aprq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aprq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aprq *AppPowerRentalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aprq.driver.Dialect())
	t1 := builder.Table(apppowerrental.Table)
	columns := aprq.fields
	if len(columns) == 0 {
		columns = apppowerrental.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aprq.sql != nil {
		selector = aprq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aprq.unique != nil && *aprq.unique {
		selector.Distinct()
	}
	for _, m := range aprq.modifiers {
		m(selector)
	}
	for _, p := range aprq.predicates {
		p(selector)
	}
	for _, p := range aprq.order {
		p(selector)
	}
	if offset := aprq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aprq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (aprq *AppPowerRentalQuery) ForUpdate(opts ...sql.LockOption) *AppPowerRentalQuery {
	if aprq.driver.Dialect() == dialect.Postgres {
		aprq.Unique(false)
	}
	aprq.modifiers = append(aprq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return aprq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (aprq *AppPowerRentalQuery) ForShare(opts ...sql.LockOption) *AppPowerRentalQuery {
	if aprq.driver.Dialect() == dialect.Postgres {
		aprq.Unique(false)
	}
	aprq.modifiers = append(aprq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return aprq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aprq *AppPowerRentalQuery) Modify(modifiers ...func(s *sql.Selector)) *AppPowerRentalSelect {
	aprq.modifiers = append(aprq.modifiers, modifiers...)
	return aprq.Select()
}

// AppPowerRentalGroupBy is the group-by builder for AppPowerRental entities.
type AppPowerRentalGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aprgb *AppPowerRentalGroupBy) Aggregate(fns ...AggregateFunc) *AppPowerRentalGroupBy {
	aprgb.fns = append(aprgb.fns, fns...)
	return aprgb
}

// Scan applies the group-by query and scans the result into the given value.
func (aprgb *AppPowerRentalGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := aprgb.path(ctx)
	if err != nil {
		return err
	}
	aprgb.sql = query
	return aprgb.sqlScan(ctx, v)
}

func (aprgb *AppPowerRentalGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range aprgb.fields {
		if !apppowerrental.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := aprgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aprgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (aprgb *AppPowerRentalGroupBy) sqlQuery() *sql.Selector {
	selector := aprgb.sql.Select()
	aggregation := make([]string, 0, len(aprgb.fns))
	for _, fn := range aprgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(aprgb.fields)+len(aprgb.fns))
		for _, f := range aprgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(aprgb.fields...)...)
}

// AppPowerRentalSelect is the builder for selecting fields of AppPowerRental entities.
type AppPowerRentalSelect struct {
	*AppPowerRentalQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (aprs *AppPowerRentalSelect) Scan(ctx context.Context, v interface{}) error {
	if err := aprs.prepareQuery(ctx); err != nil {
		return err
	}
	aprs.sql = aprs.AppPowerRentalQuery.sqlQuery(ctx)
	return aprs.sqlScan(ctx, v)
}

func (aprs *AppPowerRentalSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := aprs.sql.Query()
	if err := aprs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aprs *AppPowerRentalSelect) Modify(modifiers ...func(s *sql.Selector)) *AppPowerRentalSelect {
	aprs.modifiers = append(aprs.modifiers, modifiers...)
	return aprs
}
