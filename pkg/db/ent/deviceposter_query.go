// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/deviceposter"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// DevicePosterQuery is the builder for querying DevicePoster entities.
type DevicePosterQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.DevicePoster
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DevicePosterQuery builder.
func (dpq *DevicePosterQuery) Where(ps ...predicate.DevicePoster) *DevicePosterQuery {
	dpq.predicates = append(dpq.predicates, ps...)
	return dpq
}

// Limit adds a limit step to the query.
func (dpq *DevicePosterQuery) Limit(limit int) *DevicePosterQuery {
	dpq.limit = &limit
	return dpq
}

// Offset adds an offset step to the query.
func (dpq *DevicePosterQuery) Offset(offset int) *DevicePosterQuery {
	dpq.offset = &offset
	return dpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dpq *DevicePosterQuery) Unique(unique bool) *DevicePosterQuery {
	dpq.unique = &unique
	return dpq
}

// Order adds an order step to the query.
func (dpq *DevicePosterQuery) Order(o ...OrderFunc) *DevicePosterQuery {
	dpq.order = append(dpq.order, o...)
	return dpq
}

// First returns the first DevicePoster entity from the query.
// Returns a *NotFoundError when no DevicePoster was found.
func (dpq *DevicePosterQuery) First(ctx context.Context) (*DevicePoster, error) {
	nodes, err := dpq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deviceposter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dpq *DevicePosterQuery) FirstX(ctx context.Context) *DevicePoster {
	node, err := dpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DevicePoster ID from the query.
// Returns a *NotFoundError when no DevicePoster ID was found.
func (dpq *DevicePosterQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = dpq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deviceposter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dpq *DevicePosterQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := dpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DevicePoster entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DevicePoster entity is found.
// Returns a *NotFoundError when no DevicePoster entities are found.
func (dpq *DevicePosterQuery) Only(ctx context.Context) (*DevicePoster, error) {
	nodes, err := dpq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deviceposter.Label}
	default:
		return nil, &NotSingularError{deviceposter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dpq *DevicePosterQuery) OnlyX(ctx context.Context) *DevicePoster {
	node, err := dpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DevicePoster ID in the query.
// Returns a *NotSingularError when more than one DevicePoster ID is found.
// Returns a *NotFoundError when no entities are found.
func (dpq *DevicePosterQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = dpq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deviceposter.Label}
	default:
		err = &NotSingularError{deviceposter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dpq *DevicePosterQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := dpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DevicePosters.
func (dpq *DevicePosterQuery) All(ctx context.Context) ([]*DevicePoster, error) {
	if err := dpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return dpq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (dpq *DevicePosterQuery) AllX(ctx context.Context) []*DevicePoster {
	nodes, err := dpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DevicePoster IDs.
func (dpq *DevicePosterQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := dpq.Select(deviceposter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dpq *DevicePosterQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := dpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dpq *DevicePosterQuery) Count(ctx context.Context) (int, error) {
	if err := dpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return dpq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (dpq *DevicePosterQuery) CountX(ctx context.Context) int {
	count, err := dpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dpq *DevicePosterQuery) Exist(ctx context.Context) (bool, error) {
	if err := dpq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return dpq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (dpq *DevicePosterQuery) ExistX(ctx context.Context) bool {
	exist, err := dpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DevicePosterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dpq *DevicePosterQuery) Clone() *DevicePosterQuery {
	if dpq == nil {
		return nil
	}
	return &DevicePosterQuery{
		config:     dpq.config,
		limit:      dpq.limit,
		offset:     dpq.offset,
		order:      append([]OrderFunc{}, dpq.order...),
		predicates: append([]predicate.DevicePoster{}, dpq.predicates...),
		// clone intermediate query.
		sql:    dpq.sql.Clone(),
		path:   dpq.path,
		unique: dpq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DevicePoster.Query().
//		GroupBy(deviceposter.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (dpq *DevicePosterQuery) GroupBy(field string, fields ...string) *DevicePosterGroupBy {
	grbuild := &DevicePosterGroupBy{config: dpq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := dpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return dpq.sqlQuery(ctx), nil
	}
	grbuild.label = deviceposter.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.DevicePoster.Query().
//		Select(deviceposter.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (dpq *DevicePosterQuery) Select(fields ...string) *DevicePosterSelect {
	dpq.fields = append(dpq.fields, fields...)
	selbuild := &DevicePosterSelect{DevicePosterQuery: dpq}
	selbuild.label = deviceposter.Label
	selbuild.flds, selbuild.scan = &dpq.fields, selbuild.Scan
	return selbuild
}

func (dpq *DevicePosterQuery) prepareQuery(ctx context.Context) error {
	for _, f := range dpq.fields {
		if !deviceposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dpq.path != nil {
		prev, err := dpq.path(ctx)
		if err != nil {
			return err
		}
		dpq.sql = prev
	}
	if deviceposter.Policy == nil {
		return errors.New("ent: uninitialized deviceposter.Policy (forgotten import ent/runtime?)")
	}
	if err := deviceposter.Policy.EvalQuery(ctx, dpq); err != nil {
		return err
	}
	return nil
}

func (dpq *DevicePosterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DevicePoster, error) {
	var (
		nodes = []*DevicePoster{}
		_spec = dpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*DevicePoster).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &DevicePoster{config: dpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(dpq.modifiers) > 0 {
		_spec.Modifiers = dpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dpq *DevicePosterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dpq.querySpec()
	if len(dpq.modifiers) > 0 {
		_spec.Modifiers = dpq.modifiers
	}
	_spec.Node.Columns = dpq.fields
	if len(dpq.fields) > 0 {
		_spec.Unique = dpq.unique != nil && *dpq.unique
	}
	return sqlgraph.CountNodes(ctx, dpq.driver, _spec)
}

func (dpq *DevicePosterQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := dpq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (dpq *DevicePosterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   deviceposter.Table,
			Columns: deviceposter.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: deviceposter.FieldID,
			},
		},
		From:   dpq.sql,
		Unique: true,
	}
	if unique := dpq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := dpq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deviceposter.FieldID)
		for i := range fields {
			if fields[i] != deviceposter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dpq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dpq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dpq *DevicePosterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dpq.driver.Dialect())
	t1 := builder.Table(deviceposter.Table)
	columns := dpq.fields
	if len(columns) == 0 {
		columns = deviceposter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dpq.sql != nil {
		selector = dpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dpq.unique != nil && *dpq.unique {
		selector.Distinct()
	}
	for _, m := range dpq.modifiers {
		m(selector)
	}
	for _, p := range dpq.predicates {
		p(selector)
	}
	for _, p := range dpq.order {
		p(selector)
	}
	if offset := dpq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dpq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (dpq *DevicePosterQuery) ForUpdate(opts ...sql.LockOption) *DevicePosterQuery {
	if dpq.driver.Dialect() == dialect.Postgres {
		dpq.Unique(false)
	}
	dpq.modifiers = append(dpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return dpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (dpq *DevicePosterQuery) ForShare(opts ...sql.LockOption) *DevicePosterQuery {
	if dpq.driver.Dialect() == dialect.Postgres {
		dpq.Unique(false)
	}
	dpq.modifiers = append(dpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return dpq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dpq *DevicePosterQuery) Modify(modifiers ...func(s *sql.Selector)) *DevicePosterSelect {
	dpq.modifiers = append(dpq.modifiers, modifiers...)
	return dpq.Select()
}

// DevicePosterGroupBy is the group-by builder for DevicePoster entities.
type DevicePosterGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dpgb *DevicePosterGroupBy) Aggregate(fns ...AggregateFunc) *DevicePosterGroupBy {
	dpgb.fns = append(dpgb.fns, fns...)
	return dpgb
}

// Scan applies the group-by query and scans the result into the given value.
func (dpgb *DevicePosterGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := dpgb.path(ctx)
	if err != nil {
		return err
	}
	dpgb.sql = query
	return dpgb.sqlScan(ctx, v)
}

func (dpgb *DevicePosterGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range dpgb.fields {
		if !deviceposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := dpgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dpgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (dpgb *DevicePosterGroupBy) sqlQuery() *sql.Selector {
	selector := dpgb.sql.Select()
	aggregation := make([]string, 0, len(dpgb.fns))
	for _, fn := range dpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(dpgb.fields)+len(dpgb.fns))
		for _, f := range dpgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(dpgb.fields...)...)
}

// DevicePosterSelect is the builder for selecting fields of DevicePoster entities.
type DevicePosterSelect struct {
	*DevicePosterQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (dps *DevicePosterSelect) Scan(ctx context.Context, v interface{}) error {
	if err := dps.prepareQuery(ctx); err != nil {
		return err
	}
	dps.sql = dps.DevicePosterQuery.sqlQuery(ctx)
	return dps.sqlScan(ctx, v)
}

func (dps *DevicePosterSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := dps.sql.Query()
	if err := dps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dps *DevicePosterSelect) Modify(modifiers ...func(s *sql.Selector)) *DevicePosterSelect {
	dps.modifiers = append(dps.modifiers, modifiers...)
	return dps
}
