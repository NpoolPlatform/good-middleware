// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/appgoodposter"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// AppGoodPosterQuery is the builder for querying AppGoodPoster entities.
type AppGoodPosterQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AppGoodPoster
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppGoodPosterQuery builder.
func (agpq *AppGoodPosterQuery) Where(ps ...predicate.AppGoodPoster) *AppGoodPosterQuery {
	agpq.predicates = append(agpq.predicates, ps...)
	return agpq
}

// Limit adds a limit step to the query.
func (agpq *AppGoodPosterQuery) Limit(limit int) *AppGoodPosterQuery {
	agpq.limit = &limit
	return agpq
}

// Offset adds an offset step to the query.
func (agpq *AppGoodPosterQuery) Offset(offset int) *AppGoodPosterQuery {
	agpq.offset = &offset
	return agpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (agpq *AppGoodPosterQuery) Unique(unique bool) *AppGoodPosterQuery {
	agpq.unique = &unique
	return agpq
}

// Order adds an order step to the query.
func (agpq *AppGoodPosterQuery) Order(o ...OrderFunc) *AppGoodPosterQuery {
	agpq.order = append(agpq.order, o...)
	return agpq
}

// First returns the first AppGoodPoster entity from the query.
// Returns a *NotFoundError when no AppGoodPoster was found.
func (agpq *AppGoodPosterQuery) First(ctx context.Context) (*AppGoodPoster, error) {
	nodes, err := agpq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appgoodposter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) FirstX(ctx context.Context) *AppGoodPoster {
	node, err := agpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppGoodPoster ID from the query.
// Returns a *NotFoundError when no AppGoodPoster ID was found.
func (agpq *AppGoodPosterQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = agpq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appgoodposter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := agpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppGoodPoster entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppGoodPoster entity is found.
// Returns a *NotFoundError when no AppGoodPoster entities are found.
func (agpq *AppGoodPosterQuery) Only(ctx context.Context) (*AppGoodPoster, error) {
	nodes, err := agpq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appgoodposter.Label}
	default:
		return nil, &NotSingularError{appgoodposter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) OnlyX(ctx context.Context) *AppGoodPoster {
	node, err := agpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppGoodPoster ID in the query.
// Returns a *NotSingularError when more than one AppGoodPoster ID is found.
// Returns a *NotFoundError when no entities are found.
func (agpq *AppGoodPosterQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = agpq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appgoodposter.Label}
	default:
		err = &NotSingularError{appgoodposter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := agpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppGoodPosters.
func (agpq *AppGoodPosterQuery) All(ctx context.Context) ([]*AppGoodPoster, error) {
	if err := agpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return agpq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) AllX(ctx context.Context) []*AppGoodPoster {
	nodes, err := agpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppGoodPoster IDs.
func (agpq *AppGoodPosterQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := agpq.Select(appgoodposter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := agpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (agpq *AppGoodPosterQuery) Count(ctx context.Context) (int, error) {
	if err := agpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return agpq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) CountX(ctx context.Context) int {
	count, err := agpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (agpq *AppGoodPosterQuery) Exist(ctx context.Context) (bool, error) {
	if err := agpq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return agpq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (agpq *AppGoodPosterQuery) ExistX(ctx context.Context) bool {
	exist, err := agpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppGoodPosterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (agpq *AppGoodPosterQuery) Clone() *AppGoodPosterQuery {
	if agpq == nil {
		return nil
	}
	return &AppGoodPosterQuery{
		config:     agpq.config,
		limit:      agpq.limit,
		offset:     agpq.offset,
		order:      append([]OrderFunc{}, agpq.order...),
		predicates: append([]predicate.AppGoodPoster{}, agpq.predicates...),
		// clone intermediate query.
		sql:    agpq.sql.Clone(),
		path:   agpq.path,
		unique: agpq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppGoodPoster.Query().
//		GroupBy(appgoodposter.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (agpq *AppGoodPosterQuery) GroupBy(field string, fields ...string) *AppGoodPosterGroupBy {
	grbuild := &AppGoodPosterGroupBy{config: agpq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := agpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return agpq.sqlQuery(ctx), nil
	}
	grbuild.label = appgoodposter.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.AppGoodPoster.Query().
//		Select(appgoodposter.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (agpq *AppGoodPosterQuery) Select(fields ...string) *AppGoodPosterSelect {
	agpq.fields = append(agpq.fields, fields...)
	selbuild := &AppGoodPosterSelect{AppGoodPosterQuery: agpq}
	selbuild.label = appgoodposter.Label
	selbuild.flds, selbuild.scan = &agpq.fields, selbuild.Scan
	return selbuild
}

func (agpq *AppGoodPosterQuery) prepareQuery(ctx context.Context) error {
	for _, f := range agpq.fields {
		if !appgoodposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if agpq.path != nil {
		prev, err := agpq.path(ctx)
		if err != nil {
			return err
		}
		agpq.sql = prev
	}
	if appgoodposter.Policy == nil {
		return errors.New("ent: uninitialized appgoodposter.Policy (forgotten import ent/runtime?)")
	}
	if err := appgoodposter.Policy.EvalQuery(ctx, agpq); err != nil {
		return err
	}
	return nil
}

func (agpq *AppGoodPosterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppGoodPoster, error) {
	var (
		nodes = []*AppGoodPoster{}
		_spec = agpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*AppGoodPoster).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &AppGoodPoster{config: agpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(agpq.modifiers) > 0 {
		_spec.Modifiers = agpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, agpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (agpq *AppGoodPosterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := agpq.querySpec()
	if len(agpq.modifiers) > 0 {
		_spec.Modifiers = agpq.modifiers
	}
	_spec.Node.Columns = agpq.fields
	if len(agpq.fields) > 0 {
		_spec.Unique = agpq.unique != nil && *agpq.unique
	}
	return sqlgraph.CountNodes(ctx, agpq.driver, _spec)
}

func (agpq *AppGoodPosterQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := agpq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (agpq *AppGoodPosterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   appgoodposter.Table,
			Columns: appgoodposter.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: appgoodposter.FieldID,
			},
		},
		From:   agpq.sql,
		Unique: true,
	}
	if unique := agpq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := agpq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appgoodposter.FieldID)
		for i := range fields {
			if fields[i] != appgoodposter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := agpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := agpq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := agpq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := agpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (agpq *AppGoodPosterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(agpq.driver.Dialect())
	t1 := builder.Table(appgoodposter.Table)
	columns := agpq.fields
	if len(columns) == 0 {
		columns = appgoodposter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if agpq.sql != nil {
		selector = agpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if agpq.unique != nil && *agpq.unique {
		selector.Distinct()
	}
	for _, m := range agpq.modifiers {
		m(selector)
	}
	for _, p := range agpq.predicates {
		p(selector)
	}
	for _, p := range agpq.order {
		p(selector)
	}
	if offset := agpq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := agpq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (agpq *AppGoodPosterQuery) ForUpdate(opts ...sql.LockOption) *AppGoodPosterQuery {
	if agpq.driver.Dialect() == dialect.Postgres {
		agpq.Unique(false)
	}
	agpq.modifiers = append(agpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return agpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (agpq *AppGoodPosterQuery) ForShare(opts ...sql.LockOption) *AppGoodPosterQuery {
	if agpq.driver.Dialect() == dialect.Postgres {
		agpq.Unique(false)
	}
	agpq.modifiers = append(agpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return agpq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (agpq *AppGoodPosterQuery) Modify(modifiers ...func(s *sql.Selector)) *AppGoodPosterSelect {
	agpq.modifiers = append(agpq.modifiers, modifiers...)
	return agpq.Select()
}

// AppGoodPosterGroupBy is the group-by builder for AppGoodPoster entities.
type AppGoodPosterGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (agpgb *AppGoodPosterGroupBy) Aggregate(fns ...AggregateFunc) *AppGoodPosterGroupBy {
	agpgb.fns = append(agpgb.fns, fns...)
	return agpgb
}

// Scan applies the group-by query and scans the result into the given value.
func (agpgb *AppGoodPosterGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := agpgb.path(ctx)
	if err != nil {
		return err
	}
	agpgb.sql = query
	return agpgb.sqlScan(ctx, v)
}

func (agpgb *AppGoodPosterGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range agpgb.fields {
		if !appgoodposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := agpgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := agpgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (agpgb *AppGoodPosterGroupBy) sqlQuery() *sql.Selector {
	selector := agpgb.sql.Select()
	aggregation := make([]string, 0, len(agpgb.fns))
	for _, fn := range agpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(agpgb.fields)+len(agpgb.fns))
		for _, f := range agpgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(agpgb.fields...)...)
}

// AppGoodPosterSelect is the builder for selecting fields of AppGoodPoster entities.
type AppGoodPosterSelect struct {
	*AppGoodPosterQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (agps *AppGoodPosterSelect) Scan(ctx context.Context, v interface{}) error {
	if err := agps.prepareQuery(ctx); err != nil {
		return err
	}
	agps.sql = agps.AppGoodPosterQuery.sqlQuery(ctx)
	return agps.sqlScan(ctx, v)
}

func (agps *AppGoodPosterSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := agps.sql.Query()
	if err := agps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (agps *AppGoodPosterSelect) Modify(modifiers ...func(s *sql.Selector)) *AppGoodPosterSelect {
	agps.modifiers = append(agps.modifiers, modifiers...)
	return agps
}
