// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/goodmalfunction"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// GoodMalfunctionQuery is the builder for querying GoodMalfunction entities.
type GoodMalfunctionQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.GoodMalfunction
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GoodMalfunctionQuery builder.
func (gmq *GoodMalfunctionQuery) Where(ps ...predicate.GoodMalfunction) *GoodMalfunctionQuery {
	gmq.predicates = append(gmq.predicates, ps...)
	return gmq
}

// Limit adds a limit step to the query.
func (gmq *GoodMalfunctionQuery) Limit(limit int) *GoodMalfunctionQuery {
	gmq.limit = &limit
	return gmq
}

// Offset adds an offset step to the query.
func (gmq *GoodMalfunctionQuery) Offset(offset int) *GoodMalfunctionQuery {
	gmq.offset = &offset
	return gmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gmq *GoodMalfunctionQuery) Unique(unique bool) *GoodMalfunctionQuery {
	gmq.unique = &unique
	return gmq
}

// Order adds an order step to the query.
func (gmq *GoodMalfunctionQuery) Order(o ...OrderFunc) *GoodMalfunctionQuery {
	gmq.order = append(gmq.order, o...)
	return gmq
}

// First returns the first GoodMalfunction entity from the query.
// Returns a *NotFoundError when no GoodMalfunction was found.
func (gmq *GoodMalfunctionQuery) First(ctx context.Context) (*GoodMalfunction, error) {
	nodes, err := gmq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{goodmalfunction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) FirstX(ctx context.Context) *GoodMalfunction {
	node, err := gmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GoodMalfunction ID from the query.
// Returns a *NotFoundError when no GoodMalfunction ID was found.
func (gmq *GoodMalfunctionQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = gmq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{goodmalfunction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := gmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GoodMalfunction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GoodMalfunction entity is found.
// Returns a *NotFoundError when no GoodMalfunction entities are found.
func (gmq *GoodMalfunctionQuery) Only(ctx context.Context) (*GoodMalfunction, error) {
	nodes, err := gmq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{goodmalfunction.Label}
	default:
		return nil, &NotSingularError{goodmalfunction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) OnlyX(ctx context.Context) *GoodMalfunction {
	node, err := gmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GoodMalfunction ID in the query.
// Returns a *NotSingularError when more than one GoodMalfunction ID is found.
// Returns a *NotFoundError when no entities are found.
func (gmq *GoodMalfunctionQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = gmq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{goodmalfunction.Label}
	default:
		err = &NotSingularError{goodmalfunction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := gmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GoodMalfunctions.
func (gmq *GoodMalfunctionQuery) All(ctx context.Context) ([]*GoodMalfunction, error) {
	if err := gmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return gmq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) AllX(ctx context.Context) []*GoodMalfunction {
	nodes, err := gmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GoodMalfunction IDs.
func (gmq *GoodMalfunctionQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := gmq.Select(goodmalfunction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := gmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gmq *GoodMalfunctionQuery) Count(ctx context.Context) (int, error) {
	if err := gmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return gmq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) CountX(ctx context.Context) int {
	count, err := gmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gmq *GoodMalfunctionQuery) Exist(ctx context.Context) (bool, error) {
	if err := gmq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return gmq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (gmq *GoodMalfunctionQuery) ExistX(ctx context.Context) bool {
	exist, err := gmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GoodMalfunctionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gmq *GoodMalfunctionQuery) Clone() *GoodMalfunctionQuery {
	if gmq == nil {
		return nil
	}
	return &GoodMalfunctionQuery{
		config:     gmq.config,
		limit:      gmq.limit,
		offset:     gmq.offset,
		order:      append([]OrderFunc{}, gmq.order...),
		predicates: append([]predicate.GoodMalfunction{}, gmq.predicates...),
		// clone intermediate query.
		sql:    gmq.sql.Clone(),
		path:   gmq.path,
		unique: gmq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GoodMalfunction.Query().
//		GroupBy(goodmalfunction.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (gmq *GoodMalfunctionQuery) GroupBy(field string, fields ...string) *GoodMalfunctionGroupBy {
	grbuild := &GoodMalfunctionGroupBy{config: gmq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := gmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return gmq.sqlQuery(ctx), nil
	}
	grbuild.label = goodmalfunction.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.GoodMalfunction.Query().
//		Select(goodmalfunction.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (gmq *GoodMalfunctionQuery) Select(fields ...string) *GoodMalfunctionSelect {
	gmq.fields = append(gmq.fields, fields...)
	selbuild := &GoodMalfunctionSelect{GoodMalfunctionQuery: gmq}
	selbuild.label = goodmalfunction.Label
	selbuild.flds, selbuild.scan = &gmq.fields, selbuild.Scan
	return selbuild
}

func (gmq *GoodMalfunctionQuery) prepareQuery(ctx context.Context) error {
	for _, f := range gmq.fields {
		if !goodmalfunction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gmq.path != nil {
		prev, err := gmq.path(ctx)
		if err != nil {
			return err
		}
		gmq.sql = prev
	}
	if goodmalfunction.Policy == nil {
		return errors.New("ent: uninitialized goodmalfunction.Policy (forgotten import ent/runtime?)")
	}
	if err := goodmalfunction.Policy.EvalQuery(ctx, gmq); err != nil {
		return err
	}
	return nil
}

func (gmq *GoodMalfunctionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GoodMalfunction, error) {
	var (
		nodes = []*GoodMalfunction{}
		_spec = gmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*GoodMalfunction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &GoodMalfunction{config: gmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(gmq.modifiers) > 0 {
		_spec.Modifiers = gmq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gmq *GoodMalfunctionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gmq.querySpec()
	if len(gmq.modifiers) > 0 {
		_spec.Modifiers = gmq.modifiers
	}
	_spec.Node.Columns = gmq.fields
	if len(gmq.fields) > 0 {
		_spec.Unique = gmq.unique != nil && *gmq.unique
	}
	return sqlgraph.CountNodes(ctx, gmq.driver, _spec)
}

func (gmq *GoodMalfunctionQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := gmq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (gmq *GoodMalfunctionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   goodmalfunction.Table,
			Columns: goodmalfunction.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: goodmalfunction.FieldID,
			},
		},
		From:   gmq.sql,
		Unique: true,
	}
	if unique := gmq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := gmq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, goodmalfunction.FieldID)
		for i := range fields {
			if fields[i] != goodmalfunction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gmq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gmq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gmq *GoodMalfunctionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gmq.driver.Dialect())
	t1 := builder.Table(goodmalfunction.Table)
	columns := gmq.fields
	if len(columns) == 0 {
		columns = goodmalfunction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gmq.sql != nil {
		selector = gmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gmq.unique != nil && *gmq.unique {
		selector.Distinct()
	}
	for _, m := range gmq.modifiers {
		m(selector)
	}
	for _, p := range gmq.predicates {
		p(selector)
	}
	for _, p := range gmq.order {
		p(selector)
	}
	if offset := gmq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gmq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (gmq *GoodMalfunctionQuery) ForUpdate(opts ...sql.LockOption) *GoodMalfunctionQuery {
	if gmq.driver.Dialect() == dialect.Postgres {
		gmq.Unique(false)
	}
	gmq.modifiers = append(gmq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return gmq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (gmq *GoodMalfunctionQuery) ForShare(opts ...sql.LockOption) *GoodMalfunctionQuery {
	if gmq.driver.Dialect() == dialect.Postgres {
		gmq.Unique(false)
	}
	gmq.modifiers = append(gmq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return gmq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gmq *GoodMalfunctionQuery) Modify(modifiers ...func(s *sql.Selector)) *GoodMalfunctionSelect {
	gmq.modifiers = append(gmq.modifiers, modifiers...)
	return gmq.Select()
}

// GoodMalfunctionGroupBy is the group-by builder for GoodMalfunction entities.
type GoodMalfunctionGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gmgb *GoodMalfunctionGroupBy) Aggregate(fns ...AggregateFunc) *GoodMalfunctionGroupBy {
	gmgb.fns = append(gmgb.fns, fns...)
	return gmgb
}

// Scan applies the group-by query and scans the result into the given value.
func (gmgb *GoodMalfunctionGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := gmgb.path(ctx)
	if err != nil {
		return err
	}
	gmgb.sql = query
	return gmgb.sqlScan(ctx, v)
}

func (gmgb *GoodMalfunctionGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range gmgb.fields {
		if !goodmalfunction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := gmgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gmgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (gmgb *GoodMalfunctionGroupBy) sqlQuery() *sql.Selector {
	selector := gmgb.sql.Select()
	aggregation := make([]string, 0, len(gmgb.fns))
	for _, fn := range gmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(gmgb.fields)+len(gmgb.fns))
		for _, f := range gmgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(gmgb.fields...)...)
}

// GoodMalfunctionSelect is the builder for selecting fields of GoodMalfunction entities.
type GoodMalfunctionSelect struct {
	*GoodMalfunctionQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (gms *GoodMalfunctionSelect) Scan(ctx context.Context, v interface{}) error {
	if err := gms.prepareQuery(ctx); err != nil {
		return err
	}
	gms.sql = gms.GoodMalfunctionQuery.sqlQuery(ctx)
	return gms.sqlScan(ctx, v)
}

func (gms *GoodMalfunctionSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := gms.sql.Query()
	if err := gms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gms *GoodMalfunctionSelect) Modify(modifiers ...func(s *sql.Selector)) *GoodMalfunctionSelect {
	gms.modifiers = append(gms.modifiers, modifiers...)
	return gms
}
