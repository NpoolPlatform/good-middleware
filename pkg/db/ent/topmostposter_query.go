// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/topmostposter"
)

// TopMostPosterQuery is the builder for querying TopMostPoster entities.
type TopMostPosterQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TopMostPoster
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TopMostPosterQuery builder.
func (tmpq *TopMostPosterQuery) Where(ps ...predicate.TopMostPoster) *TopMostPosterQuery {
	tmpq.predicates = append(tmpq.predicates, ps...)
	return tmpq
}

// Limit adds a limit step to the query.
func (tmpq *TopMostPosterQuery) Limit(limit int) *TopMostPosterQuery {
	tmpq.limit = &limit
	return tmpq
}

// Offset adds an offset step to the query.
func (tmpq *TopMostPosterQuery) Offset(offset int) *TopMostPosterQuery {
	tmpq.offset = &offset
	return tmpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tmpq *TopMostPosterQuery) Unique(unique bool) *TopMostPosterQuery {
	tmpq.unique = &unique
	return tmpq
}

// Order adds an order step to the query.
func (tmpq *TopMostPosterQuery) Order(o ...OrderFunc) *TopMostPosterQuery {
	tmpq.order = append(tmpq.order, o...)
	return tmpq
}

// First returns the first TopMostPoster entity from the query.
// Returns a *NotFoundError when no TopMostPoster was found.
func (tmpq *TopMostPosterQuery) First(ctx context.Context) (*TopMostPoster, error) {
	nodes, err := tmpq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{topmostposter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) FirstX(ctx context.Context) *TopMostPoster {
	node, err := tmpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TopMostPoster ID from the query.
// Returns a *NotFoundError when no TopMostPoster ID was found.
func (tmpq *TopMostPosterQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = tmpq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{topmostposter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := tmpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TopMostPoster entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TopMostPoster entity is found.
// Returns a *NotFoundError when no TopMostPoster entities are found.
func (tmpq *TopMostPosterQuery) Only(ctx context.Context) (*TopMostPoster, error) {
	nodes, err := tmpq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{topmostposter.Label}
	default:
		return nil, &NotSingularError{topmostposter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) OnlyX(ctx context.Context) *TopMostPoster {
	node, err := tmpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TopMostPoster ID in the query.
// Returns a *NotSingularError when more than one TopMostPoster ID is found.
// Returns a *NotFoundError when no entities are found.
func (tmpq *TopMostPosterQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = tmpq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{topmostposter.Label}
	default:
		err = &NotSingularError{topmostposter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := tmpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TopMostPosters.
func (tmpq *TopMostPosterQuery) All(ctx context.Context) ([]*TopMostPoster, error) {
	if err := tmpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tmpq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) AllX(ctx context.Context) []*TopMostPoster {
	nodes, err := tmpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TopMostPoster IDs.
func (tmpq *TopMostPosterQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := tmpq.Select(topmostposter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := tmpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tmpq *TopMostPosterQuery) Count(ctx context.Context) (int, error) {
	if err := tmpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tmpq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) CountX(ctx context.Context) int {
	count, err := tmpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tmpq *TopMostPosterQuery) Exist(ctx context.Context) (bool, error) {
	if err := tmpq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tmpq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tmpq *TopMostPosterQuery) ExistX(ctx context.Context) bool {
	exist, err := tmpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TopMostPosterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tmpq *TopMostPosterQuery) Clone() *TopMostPosterQuery {
	if tmpq == nil {
		return nil
	}
	return &TopMostPosterQuery{
		config:     tmpq.config,
		limit:      tmpq.limit,
		offset:     tmpq.offset,
		order:      append([]OrderFunc{}, tmpq.order...),
		predicates: append([]predicate.TopMostPoster{}, tmpq.predicates...),
		// clone intermediate query.
		sql:    tmpq.sql.Clone(),
		path:   tmpq.path,
		unique: tmpq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TopMostPoster.Query().
//		GroupBy(topmostposter.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (tmpq *TopMostPosterQuery) GroupBy(field string, fields ...string) *TopMostPosterGroupBy {
	grbuild := &TopMostPosterGroupBy{config: tmpq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tmpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tmpq.sqlQuery(ctx), nil
	}
	grbuild.label = topmostposter.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.TopMostPoster.Query().
//		Select(topmostposter.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (tmpq *TopMostPosterQuery) Select(fields ...string) *TopMostPosterSelect {
	tmpq.fields = append(tmpq.fields, fields...)
	selbuild := &TopMostPosterSelect{TopMostPosterQuery: tmpq}
	selbuild.label = topmostposter.Label
	selbuild.flds, selbuild.scan = &tmpq.fields, selbuild.Scan
	return selbuild
}

func (tmpq *TopMostPosterQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tmpq.fields {
		if !topmostposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tmpq.path != nil {
		prev, err := tmpq.path(ctx)
		if err != nil {
			return err
		}
		tmpq.sql = prev
	}
	if topmostposter.Policy == nil {
		return errors.New("ent: uninitialized topmostposter.Policy (forgotten import ent/runtime?)")
	}
	if err := topmostposter.Policy.EvalQuery(ctx, tmpq); err != nil {
		return err
	}
	return nil
}

func (tmpq *TopMostPosterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TopMostPoster, error) {
	var (
		nodes = []*TopMostPoster{}
		_spec = tmpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*TopMostPoster).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &TopMostPoster{config: tmpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tmpq.modifiers) > 0 {
		_spec.Modifiers = tmpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tmpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tmpq *TopMostPosterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tmpq.querySpec()
	if len(tmpq.modifiers) > 0 {
		_spec.Modifiers = tmpq.modifiers
	}
	_spec.Node.Columns = tmpq.fields
	if len(tmpq.fields) > 0 {
		_spec.Unique = tmpq.unique != nil && *tmpq.unique
	}
	return sqlgraph.CountNodes(ctx, tmpq.driver, _spec)
}

func (tmpq *TopMostPosterQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := tmpq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (tmpq *TopMostPosterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   topmostposter.Table,
			Columns: topmostposter.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: topmostposter.FieldID,
			},
		},
		From:   tmpq.sql,
		Unique: true,
	}
	if unique := tmpq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tmpq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, topmostposter.FieldID)
		for i := range fields {
			if fields[i] != topmostposter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tmpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tmpq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tmpq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tmpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tmpq *TopMostPosterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tmpq.driver.Dialect())
	t1 := builder.Table(topmostposter.Table)
	columns := tmpq.fields
	if len(columns) == 0 {
		columns = topmostposter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tmpq.sql != nil {
		selector = tmpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tmpq.unique != nil && *tmpq.unique {
		selector.Distinct()
	}
	for _, m := range tmpq.modifiers {
		m(selector)
	}
	for _, p := range tmpq.predicates {
		p(selector)
	}
	for _, p := range tmpq.order {
		p(selector)
	}
	if offset := tmpq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tmpq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tmpq *TopMostPosterQuery) ForUpdate(opts ...sql.LockOption) *TopMostPosterQuery {
	if tmpq.driver.Dialect() == dialect.Postgres {
		tmpq.Unique(false)
	}
	tmpq.modifiers = append(tmpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tmpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tmpq *TopMostPosterQuery) ForShare(opts ...sql.LockOption) *TopMostPosterQuery {
	if tmpq.driver.Dialect() == dialect.Postgres {
		tmpq.Unique(false)
	}
	tmpq.modifiers = append(tmpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tmpq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tmpq *TopMostPosterQuery) Modify(modifiers ...func(s *sql.Selector)) *TopMostPosterSelect {
	tmpq.modifiers = append(tmpq.modifiers, modifiers...)
	return tmpq.Select()
}

// TopMostPosterGroupBy is the group-by builder for TopMostPoster entities.
type TopMostPosterGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tmpgb *TopMostPosterGroupBy) Aggregate(fns ...AggregateFunc) *TopMostPosterGroupBy {
	tmpgb.fns = append(tmpgb.fns, fns...)
	return tmpgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tmpgb *TopMostPosterGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := tmpgb.path(ctx)
	if err != nil {
		return err
	}
	tmpgb.sql = query
	return tmpgb.sqlScan(ctx, v)
}

func (tmpgb *TopMostPosterGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range tmpgb.fields {
		if !topmostposter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tmpgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tmpgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tmpgb *TopMostPosterGroupBy) sqlQuery() *sql.Selector {
	selector := tmpgb.sql.Select()
	aggregation := make([]string, 0, len(tmpgb.fns))
	for _, fn := range tmpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tmpgb.fields)+len(tmpgb.fns))
		for _, f := range tmpgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tmpgb.fields...)...)
}

// TopMostPosterSelect is the builder for selecting fields of TopMostPoster entities.
type TopMostPosterSelect struct {
	*TopMostPosterQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (tmps *TopMostPosterSelect) Scan(ctx context.Context, v interface{}) error {
	if err := tmps.prepareQuery(ctx); err != nil {
		return err
	}
	tmps.sql = tmps.TopMostPosterQuery.sqlQuery(ctx)
	return tmps.sqlScan(ctx, v)
}

func (tmps *TopMostPosterSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := tmps.sql.Query()
	if err := tmps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tmps *TopMostPosterSelect) Modify(modifiers ...func(s *sql.Selector)) *TopMostPosterSelect {
	tmps.modifiers = append(tmps.modifiers, modifiers...)
	return tmps
}
