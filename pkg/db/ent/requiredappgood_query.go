// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/requiredappgood"
)

// RequiredAppGoodQuery is the builder for querying RequiredAppGood entities.
type RequiredAppGoodQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.RequiredAppGood
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RequiredAppGoodQuery builder.
func (ragq *RequiredAppGoodQuery) Where(ps ...predicate.RequiredAppGood) *RequiredAppGoodQuery {
	ragq.predicates = append(ragq.predicates, ps...)
	return ragq
}

// Limit adds a limit step to the query.
func (ragq *RequiredAppGoodQuery) Limit(limit int) *RequiredAppGoodQuery {
	ragq.limit = &limit
	return ragq
}

// Offset adds an offset step to the query.
func (ragq *RequiredAppGoodQuery) Offset(offset int) *RequiredAppGoodQuery {
	ragq.offset = &offset
	return ragq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ragq *RequiredAppGoodQuery) Unique(unique bool) *RequiredAppGoodQuery {
	ragq.unique = &unique
	return ragq
}

// Order adds an order step to the query.
func (ragq *RequiredAppGoodQuery) Order(o ...OrderFunc) *RequiredAppGoodQuery {
	ragq.order = append(ragq.order, o...)
	return ragq
}

// First returns the first RequiredAppGood entity from the query.
// Returns a *NotFoundError when no RequiredAppGood was found.
func (ragq *RequiredAppGoodQuery) First(ctx context.Context) (*RequiredAppGood, error) {
	nodes, err := ragq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{requiredappgood.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) FirstX(ctx context.Context) *RequiredAppGood {
	node, err := ragq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RequiredAppGood ID from the query.
// Returns a *NotFoundError when no RequiredAppGood ID was found.
func (ragq *RequiredAppGoodQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ragq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{requiredappgood.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := ragq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RequiredAppGood entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RequiredAppGood entity is found.
// Returns a *NotFoundError when no RequiredAppGood entities are found.
func (ragq *RequiredAppGoodQuery) Only(ctx context.Context) (*RequiredAppGood, error) {
	nodes, err := ragq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{requiredappgood.Label}
	default:
		return nil, &NotSingularError{requiredappgood.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) OnlyX(ctx context.Context) *RequiredAppGood {
	node, err := ragq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RequiredAppGood ID in the query.
// Returns a *NotSingularError when more than one RequiredAppGood ID is found.
// Returns a *NotFoundError when no entities are found.
func (ragq *RequiredAppGoodQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = ragq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{requiredappgood.Label}
	default:
		err = &NotSingularError{requiredappgood.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := ragq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RequiredAppGoods.
func (ragq *RequiredAppGoodQuery) All(ctx context.Context) ([]*RequiredAppGood, error) {
	if err := ragq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ragq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) AllX(ctx context.Context) []*RequiredAppGood {
	nodes, err := ragq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RequiredAppGood IDs.
func (ragq *RequiredAppGoodQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := ragq.Select(requiredappgood.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := ragq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ragq *RequiredAppGoodQuery) Count(ctx context.Context) (int, error) {
	if err := ragq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ragq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) CountX(ctx context.Context) int {
	count, err := ragq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ragq *RequiredAppGoodQuery) Exist(ctx context.Context) (bool, error) {
	if err := ragq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ragq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ragq *RequiredAppGoodQuery) ExistX(ctx context.Context) bool {
	exist, err := ragq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RequiredAppGoodQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ragq *RequiredAppGoodQuery) Clone() *RequiredAppGoodQuery {
	if ragq == nil {
		return nil
	}
	return &RequiredAppGoodQuery{
		config:     ragq.config,
		limit:      ragq.limit,
		offset:     ragq.offset,
		order:      append([]OrderFunc{}, ragq.order...),
		predicates: append([]predicate.RequiredAppGood{}, ragq.predicates...),
		// clone intermediate query.
		sql:    ragq.sql.Clone(),
		path:   ragq.path,
		unique: ragq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RequiredAppGood.Query().
//		GroupBy(requiredappgood.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (ragq *RequiredAppGoodQuery) GroupBy(field string, fields ...string) *RequiredAppGoodGroupBy {
	grbuild := &RequiredAppGoodGroupBy{config: ragq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ragq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ragq.sqlQuery(ctx), nil
	}
	grbuild.label = requiredappgood.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.RequiredAppGood.Query().
//		Select(requiredappgood.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (ragq *RequiredAppGoodQuery) Select(fields ...string) *RequiredAppGoodSelect {
	ragq.fields = append(ragq.fields, fields...)
	selbuild := &RequiredAppGoodSelect{RequiredAppGoodQuery: ragq}
	selbuild.label = requiredappgood.Label
	selbuild.flds, selbuild.scan = &ragq.fields, selbuild.Scan
	return selbuild
}

func (ragq *RequiredAppGoodQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ragq.fields {
		if !requiredappgood.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ragq.path != nil {
		prev, err := ragq.path(ctx)
		if err != nil {
			return err
		}
		ragq.sql = prev
	}
	if requiredappgood.Policy == nil {
		return errors.New("ent: uninitialized requiredappgood.Policy (forgotten import ent/runtime?)")
	}
	if err := requiredappgood.Policy.EvalQuery(ctx, ragq); err != nil {
		return err
	}
	return nil
}

func (ragq *RequiredAppGoodQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RequiredAppGood, error) {
	var (
		nodes = []*RequiredAppGood{}
		_spec = ragq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*RequiredAppGood).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &RequiredAppGood{config: ragq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ragq.modifiers) > 0 {
		_spec.Modifiers = ragq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ragq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ragq *RequiredAppGoodQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ragq.querySpec()
	if len(ragq.modifiers) > 0 {
		_spec.Modifiers = ragq.modifiers
	}
	_spec.Node.Columns = ragq.fields
	if len(ragq.fields) > 0 {
		_spec.Unique = ragq.unique != nil && *ragq.unique
	}
	return sqlgraph.CountNodes(ctx, ragq.driver, _spec)
}

func (ragq *RequiredAppGoodQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ragq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ragq *RequiredAppGoodQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   requiredappgood.Table,
			Columns: requiredappgood.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: requiredappgood.FieldID,
			},
		},
		From:   ragq.sql,
		Unique: true,
	}
	if unique := ragq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ragq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, requiredappgood.FieldID)
		for i := range fields {
			if fields[i] != requiredappgood.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ragq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ragq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ragq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ragq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ragq *RequiredAppGoodQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ragq.driver.Dialect())
	t1 := builder.Table(requiredappgood.Table)
	columns := ragq.fields
	if len(columns) == 0 {
		columns = requiredappgood.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ragq.sql != nil {
		selector = ragq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ragq.unique != nil && *ragq.unique {
		selector.Distinct()
	}
	for _, m := range ragq.modifiers {
		m(selector)
	}
	for _, p := range ragq.predicates {
		p(selector)
	}
	for _, p := range ragq.order {
		p(selector)
	}
	if offset := ragq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ragq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ragq *RequiredAppGoodQuery) ForUpdate(opts ...sql.LockOption) *RequiredAppGoodQuery {
	if ragq.driver.Dialect() == dialect.Postgres {
		ragq.Unique(false)
	}
	ragq.modifiers = append(ragq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ragq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ragq *RequiredAppGoodQuery) ForShare(opts ...sql.LockOption) *RequiredAppGoodQuery {
	if ragq.driver.Dialect() == dialect.Postgres {
		ragq.Unique(false)
	}
	ragq.modifiers = append(ragq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ragq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ragq *RequiredAppGoodQuery) Modify(modifiers ...func(s *sql.Selector)) *RequiredAppGoodSelect {
	ragq.modifiers = append(ragq.modifiers, modifiers...)
	return ragq.Select()
}

// RequiredAppGoodGroupBy is the group-by builder for RequiredAppGood entities.
type RequiredAppGoodGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (raggb *RequiredAppGoodGroupBy) Aggregate(fns ...AggregateFunc) *RequiredAppGoodGroupBy {
	raggb.fns = append(raggb.fns, fns...)
	return raggb
}

// Scan applies the group-by query and scans the result into the given value.
func (raggb *RequiredAppGoodGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := raggb.path(ctx)
	if err != nil {
		return err
	}
	raggb.sql = query
	return raggb.sqlScan(ctx, v)
}

func (raggb *RequiredAppGoodGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range raggb.fields {
		if !requiredappgood.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := raggb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := raggb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (raggb *RequiredAppGoodGroupBy) sqlQuery() *sql.Selector {
	selector := raggb.sql.Select()
	aggregation := make([]string, 0, len(raggb.fns))
	for _, fn := range raggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(raggb.fields)+len(raggb.fns))
		for _, f := range raggb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(raggb.fields...)...)
}

// RequiredAppGoodSelect is the builder for selecting fields of RequiredAppGood entities.
type RequiredAppGoodSelect struct {
	*RequiredAppGoodQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (rags *RequiredAppGoodSelect) Scan(ctx context.Context, v interface{}) error {
	if err := rags.prepareQuery(ctx); err != nil {
		return err
	}
	rags.sql = rags.RequiredAppGoodQuery.sqlQuery(ctx)
	return rags.sqlScan(ctx, v)
}

func (rags *RequiredAppGoodSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := rags.sql.Query()
	if err := rags.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rags *RequiredAppGoodSelect) Modify(modifiers ...func(s *sql.Selector)) *RequiredAppGoodSelect {
	rags.modifiers = append(rags.modifiers, modifiers...)
	return rags
}
