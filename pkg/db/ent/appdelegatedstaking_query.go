// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/appdelegatedstaking"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
)

// AppDelegatedStakingQuery is the builder for querying AppDelegatedStaking entities.
type AppDelegatedStakingQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AppDelegatedStaking
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppDelegatedStakingQuery builder.
func (adsq *AppDelegatedStakingQuery) Where(ps ...predicate.AppDelegatedStaking) *AppDelegatedStakingQuery {
	adsq.predicates = append(adsq.predicates, ps...)
	return adsq
}

// Limit adds a limit step to the query.
func (adsq *AppDelegatedStakingQuery) Limit(limit int) *AppDelegatedStakingQuery {
	adsq.limit = &limit
	return adsq
}

// Offset adds an offset step to the query.
func (adsq *AppDelegatedStakingQuery) Offset(offset int) *AppDelegatedStakingQuery {
	adsq.offset = &offset
	return adsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (adsq *AppDelegatedStakingQuery) Unique(unique bool) *AppDelegatedStakingQuery {
	adsq.unique = &unique
	return adsq
}

// Order adds an order step to the query.
func (adsq *AppDelegatedStakingQuery) Order(o ...OrderFunc) *AppDelegatedStakingQuery {
	adsq.order = append(adsq.order, o...)
	return adsq
}

// First returns the first AppDelegatedStaking entity from the query.
// Returns a *NotFoundError when no AppDelegatedStaking was found.
func (adsq *AppDelegatedStakingQuery) First(ctx context.Context) (*AppDelegatedStaking, error) {
	nodes, err := adsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appdelegatedstaking.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) FirstX(ctx context.Context) *AppDelegatedStaking {
	node, err := adsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppDelegatedStaking ID from the query.
// Returns a *NotFoundError when no AppDelegatedStaking ID was found.
func (adsq *AppDelegatedStakingQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = adsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appdelegatedstaking.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := adsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppDelegatedStaking entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppDelegatedStaking entity is found.
// Returns a *NotFoundError when no AppDelegatedStaking entities are found.
func (adsq *AppDelegatedStakingQuery) Only(ctx context.Context) (*AppDelegatedStaking, error) {
	nodes, err := adsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appdelegatedstaking.Label}
	default:
		return nil, &NotSingularError{appdelegatedstaking.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) OnlyX(ctx context.Context) *AppDelegatedStaking {
	node, err := adsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppDelegatedStaking ID in the query.
// Returns a *NotSingularError when more than one AppDelegatedStaking ID is found.
// Returns a *NotFoundError when no entities are found.
func (adsq *AppDelegatedStakingQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = adsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appdelegatedstaking.Label}
	default:
		err = &NotSingularError{appdelegatedstaking.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := adsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppDelegatedStakings.
func (adsq *AppDelegatedStakingQuery) All(ctx context.Context) ([]*AppDelegatedStaking, error) {
	if err := adsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return adsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) AllX(ctx context.Context) []*AppDelegatedStaking {
	nodes, err := adsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppDelegatedStaking IDs.
func (adsq *AppDelegatedStakingQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := adsq.Select(appdelegatedstaking.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := adsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (adsq *AppDelegatedStakingQuery) Count(ctx context.Context) (int, error) {
	if err := adsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return adsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) CountX(ctx context.Context) int {
	count, err := adsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (adsq *AppDelegatedStakingQuery) Exist(ctx context.Context) (bool, error) {
	if err := adsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return adsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (adsq *AppDelegatedStakingQuery) ExistX(ctx context.Context) bool {
	exist, err := adsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppDelegatedStakingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (adsq *AppDelegatedStakingQuery) Clone() *AppDelegatedStakingQuery {
	if adsq == nil {
		return nil
	}
	return &AppDelegatedStakingQuery{
		config:     adsq.config,
		limit:      adsq.limit,
		offset:     adsq.offset,
		order:      append([]OrderFunc{}, adsq.order...),
		predicates: append([]predicate.AppDelegatedStaking{}, adsq.predicates...),
		// clone intermediate query.
		sql:    adsq.sql.Clone(),
		path:   adsq.path,
		unique: adsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppDelegatedStaking.Query().
//		GroupBy(appdelegatedstaking.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (adsq *AppDelegatedStakingQuery) GroupBy(field string, fields ...string) *AppDelegatedStakingGroupBy {
	grbuild := &AppDelegatedStakingGroupBy{config: adsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := adsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return adsq.sqlQuery(ctx), nil
	}
	grbuild.label = appdelegatedstaking.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.AppDelegatedStaking.Query().
//		Select(appdelegatedstaking.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (adsq *AppDelegatedStakingQuery) Select(fields ...string) *AppDelegatedStakingSelect {
	adsq.fields = append(adsq.fields, fields...)
	selbuild := &AppDelegatedStakingSelect{AppDelegatedStakingQuery: adsq}
	selbuild.label = appdelegatedstaking.Label
	selbuild.flds, selbuild.scan = &adsq.fields, selbuild.Scan
	return selbuild
}

func (adsq *AppDelegatedStakingQuery) prepareQuery(ctx context.Context) error {
	for _, f := range adsq.fields {
		if !appdelegatedstaking.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if adsq.path != nil {
		prev, err := adsq.path(ctx)
		if err != nil {
			return err
		}
		adsq.sql = prev
	}
	if appdelegatedstaking.Policy == nil {
		return errors.New("ent: uninitialized appdelegatedstaking.Policy (forgotten import ent/runtime?)")
	}
	if err := appdelegatedstaking.Policy.EvalQuery(ctx, adsq); err != nil {
		return err
	}
	return nil
}

func (adsq *AppDelegatedStakingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppDelegatedStaking, error) {
	var (
		nodes = []*AppDelegatedStaking{}
		_spec = adsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*AppDelegatedStaking).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &AppDelegatedStaking{config: adsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(adsq.modifiers) > 0 {
		_spec.Modifiers = adsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, adsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (adsq *AppDelegatedStakingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := adsq.querySpec()
	if len(adsq.modifiers) > 0 {
		_spec.Modifiers = adsq.modifiers
	}
	_spec.Node.Columns = adsq.fields
	if len(adsq.fields) > 0 {
		_spec.Unique = adsq.unique != nil && *adsq.unique
	}
	return sqlgraph.CountNodes(ctx, adsq.driver, _spec)
}

func (adsq *AppDelegatedStakingQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := adsq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (adsq *AppDelegatedStakingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   appdelegatedstaking.Table,
			Columns: appdelegatedstaking.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: appdelegatedstaking.FieldID,
			},
		},
		From:   adsq.sql,
		Unique: true,
	}
	if unique := adsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := adsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appdelegatedstaking.FieldID)
		for i := range fields {
			if fields[i] != appdelegatedstaking.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := adsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := adsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := adsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := adsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (adsq *AppDelegatedStakingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(adsq.driver.Dialect())
	t1 := builder.Table(appdelegatedstaking.Table)
	columns := adsq.fields
	if len(columns) == 0 {
		columns = appdelegatedstaking.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if adsq.sql != nil {
		selector = adsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if adsq.unique != nil && *adsq.unique {
		selector.Distinct()
	}
	for _, m := range adsq.modifiers {
		m(selector)
	}
	for _, p := range adsq.predicates {
		p(selector)
	}
	for _, p := range adsq.order {
		p(selector)
	}
	if offset := adsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := adsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (adsq *AppDelegatedStakingQuery) ForUpdate(opts ...sql.LockOption) *AppDelegatedStakingQuery {
	if adsq.driver.Dialect() == dialect.Postgres {
		adsq.Unique(false)
	}
	adsq.modifiers = append(adsq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return adsq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (adsq *AppDelegatedStakingQuery) ForShare(opts ...sql.LockOption) *AppDelegatedStakingQuery {
	if adsq.driver.Dialect() == dialect.Postgres {
		adsq.Unique(false)
	}
	adsq.modifiers = append(adsq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return adsq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (adsq *AppDelegatedStakingQuery) Modify(modifiers ...func(s *sql.Selector)) *AppDelegatedStakingSelect {
	adsq.modifiers = append(adsq.modifiers, modifiers...)
	return adsq.Select()
}

// AppDelegatedStakingGroupBy is the group-by builder for AppDelegatedStaking entities.
type AppDelegatedStakingGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (adsgb *AppDelegatedStakingGroupBy) Aggregate(fns ...AggregateFunc) *AppDelegatedStakingGroupBy {
	adsgb.fns = append(adsgb.fns, fns...)
	return adsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (adsgb *AppDelegatedStakingGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := adsgb.path(ctx)
	if err != nil {
		return err
	}
	adsgb.sql = query
	return adsgb.sqlScan(ctx, v)
}

func (adsgb *AppDelegatedStakingGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range adsgb.fields {
		if !appdelegatedstaking.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := adsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := adsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (adsgb *AppDelegatedStakingGroupBy) sqlQuery() *sql.Selector {
	selector := adsgb.sql.Select()
	aggregation := make([]string, 0, len(adsgb.fns))
	for _, fn := range adsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(adsgb.fields)+len(adsgb.fns))
		for _, f := range adsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(adsgb.fields...)...)
}

// AppDelegatedStakingSelect is the builder for selecting fields of AppDelegatedStaking entities.
type AppDelegatedStakingSelect struct {
	*AppDelegatedStakingQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (adss *AppDelegatedStakingSelect) Scan(ctx context.Context, v interface{}) error {
	if err := adss.prepareQuery(ctx); err != nil {
		return err
	}
	adss.sql = adss.AppDelegatedStakingQuery.sqlQuery(ctx)
	return adss.sqlScan(ctx, v)
}

func (adss *AppDelegatedStakingSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := adss.sql.Query()
	if err := adss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (adss *AppDelegatedStakingSelect) Modify(modifiers ...func(s *sql.Selector)) *AppDelegatedStakingSelect {
	adss.modifiers = append(adss.modifiers, modifiers...)
	return adss
}
