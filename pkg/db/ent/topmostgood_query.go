// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/good-middleware/pkg/db/ent/topmostgood"
)

// TopMostGoodQuery is the builder for querying TopMostGood entities.
type TopMostGoodQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TopMostGood
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TopMostGoodQuery builder.
func (tmgq *TopMostGoodQuery) Where(ps ...predicate.TopMostGood) *TopMostGoodQuery {
	tmgq.predicates = append(tmgq.predicates, ps...)
	return tmgq
}

// Limit adds a limit step to the query.
func (tmgq *TopMostGoodQuery) Limit(limit int) *TopMostGoodQuery {
	tmgq.limit = &limit
	return tmgq
}

// Offset adds an offset step to the query.
func (tmgq *TopMostGoodQuery) Offset(offset int) *TopMostGoodQuery {
	tmgq.offset = &offset
	return tmgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tmgq *TopMostGoodQuery) Unique(unique bool) *TopMostGoodQuery {
	tmgq.unique = &unique
	return tmgq
}

// Order adds an order step to the query.
func (tmgq *TopMostGoodQuery) Order(o ...OrderFunc) *TopMostGoodQuery {
	tmgq.order = append(tmgq.order, o...)
	return tmgq
}

// First returns the first TopMostGood entity from the query.
// Returns a *NotFoundError when no TopMostGood was found.
func (tmgq *TopMostGoodQuery) First(ctx context.Context) (*TopMostGood, error) {
	nodes, err := tmgq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{topmostgood.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) FirstX(ctx context.Context) *TopMostGood {
	node, err := tmgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TopMostGood ID from the query.
// Returns a *NotFoundError when no TopMostGood ID was found.
func (tmgq *TopMostGoodQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = tmgq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{topmostgood.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := tmgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TopMostGood entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TopMostGood entity is found.
// Returns a *NotFoundError when no TopMostGood entities are found.
func (tmgq *TopMostGoodQuery) Only(ctx context.Context) (*TopMostGood, error) {
	nodes, err := tmgq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{topmostgood.Label}
	default:
		return nil, &NotSingularError{topmostgood.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) OnlyX(ctx context.Context) *TopMostGood {
	node, err := tmgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TopMostGood ID in the query.
// Returns a *NotSingularError when more than one TopMostGood ID is found.
// Returns a *NotFoundError when no entities are found.
func (tmgq *TopMostGoodQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = tmgq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{topmostgood.Label}
	default:
		err = &NotSingularError{topmostgood.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := tmgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TopMostGoods.
func (tmgq *TopMostGoodQuery) All(ctx context.Context) ([]*TopMostGood, error) {
	if err := tmgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tmgq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) AllX(ctx context.Context) []*TopMostGood {
	nodes, err := tmgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TopMostGood IDs.
func (tmgq *TopMostGoodQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := tmgq.Select(topmostgood.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := tmgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tmgq *TopMostGoodQuery) Count(ctx context.Context) (int, error) {
	if err := tmgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tmgq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) CountX(ctx context.Context) int {
	count, err := tmgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tmgq *TopMostGoodQuery) Exist(ctx context.Context) (bool, error) {
	if err := tmgq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tmgq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tmgq *TopMostGoodQuery) ExistX(ctx context.Context) bool {
	exist, err := tmgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TopMostGoodQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tmgq *TopMostGoodQuery) Clone() *TopMostGoodQuery {
	if tmgq == nil {
		return nil
	}
	return &TopMostGoodQuery{
		config:     tmgq.config,
		limit:      tmgq.limit,
		offset:     tmgq.offset,
		order:      append([]OrderFunc{}, tmgq.order...),
		predicates: append([]predicate.TopMostGood{}, tmgq.predicates...),
		// clone intermediate query.
		sql:    tmgq.sql.Clone(),
		path:   tmgq.path,
		unique: tmgq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TopMostGood.Query().
//		GroupBy(topmostgood.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tmgq *TopMostGoodQuery) GroupBy(field string, fields ...string) *TopMostGoodGroupBy {
	grbuild := &TopMostGoodGroupBy{config: tmgq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tmgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tmgq.sqlQuery(ctx), nil
	}
	grbuild.label = topmostgood.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.TopMostGood.Query().
//		Select(topmostgood.FieldCreatedAt).
//		Scan(ctx, &v)
func (tmgq *TopMostGoodQuery) Select(fields ...string) *TopMostGoodSelect {
	tmgq.fields = append(tmgq.fields, fields...)
	selbuild := &TopMostGoodSelect{TopMostGoodQuery: tmgq}
	selbuild.label = topmostgood.Label
	selbuild.flds, selbuild.scan = &tmgq.fields, selbuild.Scan
	return selbuild
}

func (tmgq *TopMostGoodQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tmgq.fields {
		if !topmostgood.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tmgq.path != nil {
		prev, err := tmgq.path(ctx)
		if err != nil {
			return err
		}
		tmgq.sql = prev
	}
	if topmostgood.Policy == nil {
		return errors.New("ent: uninitialized topmostgood.Policy (forgotten import ent/runtime?)")
	}
	if err := topmostgood.Policy.EvalQuery(ctx, tmgq); err != nil {
		return err
	}
	return nil
}

func (tmgq *TopMostGoodQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TopMostGood, error) {
	var (
		nodes = []*TopMostGood{}
		_spec = tmgq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*TopMostGood).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &TopMostGood{config: tmgq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tmgq.modifiers) > 0 {
		_spec.Modifiers = tmgq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tmgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tmgq *TopMostGoodQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tmgq.querySpec()
	if len(tmgq.modifiers) > 0 {
		_spec.Modifiers = tmgq.modifiers
	}
	_spec.Node.Columns = tmgq.fields
	if len(tmgq.fields) > 0 {
		_spec.Unique = tmgq.unique != nil && *tmgq.unique
	}
	return sqlgraph.CountNodes(ctx, tmgq.driver, _spec)
}

func (tmgq *TopMostGoodQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := tmgq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (tmgq *TopMostGoodQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   topmostgood.Table,
			Columns: topmostgood.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: topmostgood.FieldID,
			},
		},
		From:   tmgq.sql,
		Unique: true,
	}
	if unique := tmgq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tmgq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, topmostgood.FieldID)
		for i := range fields {
			if fields[i] != topmostgood.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tmgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tmgq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tmgq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tmgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tmgq *TopMostGoodQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tmgq.driver.Dialect())
	t1 := builder.Table(topmostgood.Table)
	columns := tmgq.fields
	if len(columns) == 0 {
		columns = topmostgood.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tmgq.sql != nil {
		selector = tmgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tmgq.unique != nil && *tmgq.unique {
		selector.Distinct()
	}
	for _, m := range tmgq.modifiers {
		m(selector)
	}
	for _, p := range tmgq.predicates {
		p(selector)
	}
	for _, p := range tmgq.order {
		p(selector)
	}
	if offset := tmgq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tmgq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tmgq *TopMostGoodQuery) ForUpdate(opts ...sql.LockOption) *TopMostGoodQuery {
	if tmgq.driver.Dialect() == dialect.Postgres {
		tmgq.Unique(false)
	}
	tmgq.modifiers = append(tmgq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tmgq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tmgq *TopMostGoodQuery) ForShare(opts ...sql.LockOption) *TopMostGoodQuery {
	if tmgq.driver.Dialect() == dialect.Postgres {
		tmgq.Unique(false)
	}
	tmgq.modifiers = append(tmgq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tmgq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tmgq *TopMostGoodQuery) Modify(modifiers ...func(s *sql.Selector)) *TopMostGoodSelect {
	tmgq.modifiers = append(tmgq.modifiers, modifiers...)
	return tmgq.Select()
}

// TopMostGoodGroupBy is the group-by builder for TopMostGood entities.
type TopMostGoodGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tmggb *TopMostGoodGroupBy) Aggregate(fns ...AggregateFunc) *TopMostGoodGroupBy {
	tmggb.fns = append(tmggb.fns, fns...)
	return tmggb
}

// Scan applies the group-by query and scans the result into the given value.
func (tmggb *TopMostGoodGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := tmggb.path(ctx)
	if err != nil {
		return err
	}
	tmggb.sql = query
	return tmggb.sqlScan(ctx, v)
}

func (tmggb *TopMostGoodGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range tmggb.fields {
		if !topmostgood.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tmggb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tmggb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tmggb *TopMostGoodGroupBy) sqlQuery() *sql.Selector {
	selector := tmggb.sql.Select()
	aggregation := make([]string, 0, len(tmggb.fns))
	for _, fn := range tmggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tmggb.fields)+len(tmggb.fns))
		for _, f := range tmggb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tmggb.fields...)...)
}

// TopMostGoodSelect is the builder for selecting fields of TopMostGood entities.
type TopMostGoodSelect struct {
	*TopMostGoodQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (tmgs *TopMostGoodSelect) Scan(ctx context.Context, v interface{}) error {
	if err := tmgs.prepareQuery(ctx); err != nil {
		return err
	}
	tmgs.sql = tmgs.TopMostGoodQuery.sqlQuery(ctx)
	return tmgs.sqlScan(ctx, v)
}

func (tmgs *TopMostGoodSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := tmgs.sql.Query()
	if err := tmgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tmgs *TopMostGoodSelect) Modify(modifiers ...func(s *sql.Selector)) *TopMostGoodSelect {
	tmgs.modifiers = append(tmgs.modifiers, modifiers...)
	return tmgs
}
